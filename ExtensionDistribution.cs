namespace DevThink
{
    /// <summary>Identifies the NuGet content package that carries the DevThink browser extension ZIP.</summary>
    public static class ExtensionDistribution
    {
        /// <summary>The canonical npm package coordinate for the associated extension distribution.</summary>
        public const string NpmPackage = "@wenathlan/devthink";
    }
}
