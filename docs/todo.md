# Project TODO

- [x] Read the supplied architecture skill and project attachments in order.
- [x] Inspect every pre-existing repository file in the selected repository.
- [x] Initialize the devthink managed web project with authentication, database, and server support.
- [x] Define typed domain models for policies, connections, sessions, plans, checkpoints, executions, diagnostics, and audit events.
- [x] Define a permission model with explicit pairing, site scope, visible activity, revocation, human approval, cancellation, and emergency stop.
- [x] Create database schema and migrations for user-scoped automation records.
- [x] Implement typed backend procedures for dashboard data, plan review, approvals, cancellation, and audit trail retrieval.
- [x] Build a responsive dashboard showing connection, session, execution, diagnostics, and security states.
- [x] Build a plan-review flow that prevents browser execution before explicit user approval.
- [x] Build an approval queue for sensitive, destructive, or otherwise non-preapproved actions.
- [x] 1.1.73: implement the agentwork module with spawnsubagent under the user depth limit, the parent scope copy with narrowing, aggregatereport merges with conflict policy order and escalation, interleaved timelines with lanes, lessonshare with matching, reuse, decay and sanitization, arbitration cases with lane verdicts and release, prioritylane ranking with the interactive lane and round robin fallback, scaleworkers load reports with consented suggestions and the shared cost ledger with the split among causers.
- [x] 1.1.73: wire the work command family into the background (spawn through the agent endpoint contract, the cancelled parent stopping its spawned children, the aggregation auto build on completion, the verdict release on holder finish, the queue drain honoring the lane order, the load samples during navigation waits, the cost entry per executed step), persist the work family through the memory seam, document the spawn, lane, lesson and cost grammar in the protocol, and render the agent work section in the sidepanel with the worker count per origin in the popup.
- [x] 1.1.74: implement the navigation module with navintent predictions ranked by step order and urlhistory, prefetchpage warming the granted urls through speculative dns with plan change dropping, preconnectorigin sockets under the host grants, deeplinkapp route patterns, reopentab grant rechecks over the closedtabrecords, restoretrail from the urlhistory with folding and replay, pausenavconsent queueing, navratelimit sliding windows per domain, openclipboardurl behind the gesture, checksafeurl lookalike verdicts and batchopenlinks with one tab per link across the rate windows.
- [x] 1.1.74: wire the nav command family into the background (the closedtabrecord on every tab removal, the navintent predictions after plan approval, the preconnect targets at session start, the sliding rate windows before every navigate step with the wait in the response envelope, the checksafeurl verdicts on every opening, the batch ceiling by user choice only), persist the navigation family through the memory seam, document the navintent prediction schema and the deeplinkpattern grammar in the protocol, and render the navigation intelligence section in the sidepanel with the closed tabs list in the popup.
- [x] 1.1.75: implement the pipelines module with streamdisk chunked writes checkpointing the streamcursor after every chunk, resumeextract skipping the persisted rows while refusing a finished pipeline, a foreign cursor, another plan or another origin, transformvalues with the raw value beside the transformed one, deduperows under the configured normalization with dropped reports, samplerows first, seeded random and stratified strategies without mutation, sourcestamp cell links with immutable timestamps, gridpreview kind inference with sorting, filtering and marks, the append only provlog with secret redaction and provenance queries by row key, and pipelinestate per run.
- [x] 1.1.75: run the extract steps through runpipelinepass in the background (the pipelinetargetgate refusal before any value persists, the stamping, the reviewed transforms before persistence, the chunked stream through the reviewed sink with the cursor checkpointed, one provlog entry per stream and transform pass, the dedupe report in the response envelope, the preview batches under the user retention and the export through the reviewed csv json and excel flows with the provgate provenance check), persist the pipeline family through the memory seam, document the extractpipeline schema and the transformrule grammar in the protocol, and render the data pipelines section in the sidepanel with the active pipeline count in the popup.
- [x] 1.1.76: implement the webapi module with subevents parsing event, data, id and retry fields with the last event id resume and the clean cancel, longpoll retrying timeouts under the user backoff with the stop conditions, the graphql-ws subscribe frame and the next error complete message mapping, formpost urlencoded encoding and multipartpost chunked uploads with progress, correlateids assignment with pair joining and the read only export, ratelimitdirectiveof parsing with ratelimitrespect waits and cacheresponse with the run namespaced keys, credential refusal and mutation invalidation.
- [x] 1.1.76: wire the transport executors through the webapi module (the stream open and poll fetch seams, the transport and upload gates, the correlation ids on every outbound request, the rate directives parsed from the response headers with the wait slept before the next call of the origin, the repeat reads served through the per run cache with the hit recorded in the step details, the observed api calls beside the discovered api map and every open transport closed on cancel and killswitch), persist the transport family through the memory seam, document the eventsubscription, graphqlsubscription, longpollrequest and multipartfield grammars with the required cancellation path in the protocol, and render the web api transports section in the sidepanel with the open transport count in the popup.
- [x] 1.1.77: implement the vision module with imageocr word boxes and confidences merged into lines and paragraphs by their own geometry, regionocr clamped regions with absolute word boxes, pdfocr page streaming through the streamcursor checkpoints, frameocr pause enforcement with the seek inside the user wait budget, visionshot reviewed prompts through the model seam, cropshot device pixel ratio scaling, redactshot masking through the fill seam with the field shape proposals, groundshot text and geometry scoring with ranked selectors, pairshot nearest same viewport dom pairing with pairquery both side search, the visioncache serving by image hash under the user retention and visioncost counting the model calls for the costshare ledger.
- [x] 1.1.77: wire the seeing surface through the vision seams (the ocr and vision passes on the stored captures with the cache serving first, the pairshot pairing beside every screenshot, the cropshot geometry on element shots, frameocr on paused videos through the page bridge videostate read, the regionocr, pdfocr, visionshot, ground, mask, propose, redact, query, cachecleanup and cost branches of the vision command family, the observation vision block and the per call sidepanel reports), persist the vision family through the memory seam with the model provider backend documented, validate the vision prompts at the protocol boundary with the capability report in the requestbody, and render the vision and ocr section in the sidepanel with the vision model status in the popup.
- [x] 1.1.78: implement the forensics module with beforeafter pairing that skips the pre capture for read only steps and links both captures to the stepid under the run heartbeat, consoletimeline capture order collection with the active stepid and the run wide sequence numbers preserved across page reloads, nettimeline step attachment with the correlateids request and response joining, diffbase page state freezing under the user threshold, diffshot pixel block regions with the similarity score and the regression flag answering the user threshold alone, thumbshot user edge sizing with the aspect held, timelapse interval planning with the explicit start and the ordered frame assembly, namecaptures lowercase pattern stamping with the counter uniqueness, and exportcaptures bundling with the provlog provenance and the redactshot masks aboard.
- [x] 1.1.78: wire the evidence surface through the forensic seams (the runstepwithforensics wrapper around every executed step with the pre and post captures, the post thumbnail and the sensitive step diffshot with the diffscore of the last step in the proposal requestbody, the console flush of the page bridge pending entries on step completion, the net trace recording of the observed exchanges through the correlation join, the timelapse loop stopped on the plan completion, the baseline, diff, thumb, timelapse, name, export, cleanup and settings branches of the forensics command family), persist the forensic family through the memory seam with the capture store backend documented, validate the capture options at the protocol boundary with the export refusal without provenance, and render the capture forensics section in the sidepanel with the forensic evidence line in the popup.
- [x] 1.1.79: implement the minimization module with localfirst extraction processing that keeps the aggregation and diffing on the device while the identity fields strip unless the review listed them and the localrule fields of the origin never leave, the telemetry policy fixed to off by construction, optinsync per data class with the consent stamps, encryptsync key derivation and payload encryption with the format tag and the passphrase refusal, purgeonrequest scoped deletion with the typed confirmation and the audit hashes surviving, exportall bundling with the uncapped streaming, the cookie jar per task run with the jargate scoping and the completion sealing under the user expiry window, cleanupafterrun artifact clearing with the retention flags, and enforcequarantine holding downloads until the scanner verdict releases or deletes them.
- [x] 1.1.79: wire the minimization surface through the seams (the extractapi executor stripping through localfirst with the report in the step details, the cookie executor routing every cookie step through the jar of its run, the run open assigning the jar beside the run record with its artifacts list, the plan completion sealing the jar and running the afterrun cleanup, the quarantinedownload and scanvirus executors building their entries through enforcequarantine with the verdict report, and the view, sync, purge, exportall, jar, quarantine, cleanup, localrule, schedule, retain and settings branches of the minimization command family), persist the minimization family through the memory seam with the encrypted sync backend documented, validate the sync payload, exportall bundle and quarantine verdict schemas at the protocol boundary with the jarid in the session records, and render the data minimization section in the sidepanel with the telemetry line fixed to off in the popup.
- [x] 1.1.80: implement the clitools module with the deep manifest checks (allowlist keys, permission source lines, csp script hash verification against the bundled bytes, reviewed web resources, duplicate permissions, the capability chrome floor and the icon dimensions), the planlint findings sharing the kind catalog with policy, the plan risk summary, the workflow document schemastrict parsing with the exit code mapping, the exportdata windows with the secret store refusal and the headless fixture replay with the consent gates and the unsupported kind reporting.
- [x] 1.1.80: wire the cli commands through the shared modules (manifest deep checks, planlint, runworkflow with the streamed progress, the review gate, the sealed audit trail, the dry run mode and the checkpoint resume, exportdata through the shared serializers, headlessmode through the library entry point with the run handle), ship the docs/examples directory with the sample plans, workflows and fixtures, add the cli smoke step to the validate chain with the verify and release workflow coverage, and document every global flag and exit code in the configuration page and the readme command table.
- [x] 1.1.81: implement the library modes with the cjs build wrapping the same named exports behind a stable getter object, the umd bundle exposing the window.devthink global with the consent gates intact, the declaration files shipping one per public module with the doc comments, the neutral target keeping every platform access behind the injected adapter seam (storage, clock, logger, fetch) with the node, browser and deno default adapters, the shared state module avoiding the dual package hazard, the version stamp, license banner, sourcemaps and minified variants on every target, the bundlestamp reporting the mode and target, the deprecation shim and the export conditions for import, require, types, default, browser, per module, cli and headless.
- [x] 1.1.81: verify the runtimes with the build matrix under node, bun and deno in the verify workflow (the require smoke against dist/index.cjs, the import smoke against dist/index.js, the global smoke against dist/devthink.umd.js, the deno check over the deno entry and the vitest suite once under bun), the umd and declaration bundles as release assets, the cjs require call in the containerfile, the checksums file for every dist target, the api surface snapshot, the interop consistency and the per target size budgets, and ship one runnable example per consumption mode in docs/examples.
- [x] 1.1.82: implement the servercontract module with the versioned envelope and the capability negotiation, the sessioncreate, sessionjoin, eventpost and eventstream operations, the chat, plan proposal, plan review and progress event types, the schema validation rejections and the stable operation ids; the socketrelay client with the exponential backoff reconnect, the stream multiplexing and the frame authentication; the sharedauth pairing code mint with the countdown, the single use exchange, the token rotation on every reconnect, the origin scoped storage and the one click revocation; and the chatbridge widget helpers that render the conversation and the review cards while the extension stays the only executor.
- [x] 1.1.82: ship the static site with hashed assets and an immutable cache configuration (zero server functions, no edge functions, no redirect rules, no vendor runtime), the serverurl setting with no default value and the wss shape validation, the bridge consent gate before the first connection, the origin checks with the external connect allowlist and the per session rate caps, the audit trail of every bridge event, the data minimization that keeps page content off the wire without the explicit consent flag, the offline queue with the replay deduplication, the heartbeat frames with the idle expiry, the kill switch, the popup bridge states, the testrelay helper for the e2e round trip, and the site zip release asset beside the extension artifacts.
- [x] 1.1.83: implement the gateway module with the provider adapter interface sharing one request, stream and cancel contract, the openaicompat chat completions adapter with streamed deltas, the anthropicgateway messages adapter with streamed content blocks and the system prompt mapping, the geminigateway generate content adapter with streamed candidate parts and the tool schema mapping, the ollamalocal localhost adapter with the local model listing, the modellist discovery under the user cache window, the capabilityad tool catalog advertisement with the consent metadata, the baseurlconfig validation, the budget tracking through the costbudget gate and the guardrails on every parsed response.
- [x] 1.1.83: wire the gateway through the llm provider model and the background task machinery with the keys in the keyvault behind the store, resolve and revoke seams, the keyexportcheck refusal, the masked request logs, the routing table with the fallback pairs and the local provider standing in, the options page configuration with the consent gate before the first remote call, the timeout, retry, backoff and jitter windows as user choices, the hardcoded provider url scan of the shipped sources, the api key literal scan of the built bundles, and the fixture server e2e that runs the full request cycle offline.
- [x] 1.1.84: implement the mcp server mode with the json rpc framing layer, the toolcatalog mapping the action kind catalog to mcp tools with json schema inputs, domain namespaces, versions, consent metadata and openapi style descriptions, the resourceexpose surfaces with the subscriptions and change notifications, the promptexpose templates, the stdioserve transport over stdin and stdout, the httpserve transport on localhost with tls and the auth handshake, the sampling callbacks, the streaming partial results, the progress notifications, the cancellation, the idempotency keys, the batch execution, the dry run mode and the tool mocks.
- [x] 1.1.84: wire the serve command through the cli with the mcpmode entry point (capability negotiation, server metadata, client sessions with isolated bindings, the allowlist, the rate caps, the degraded read only mode, the shutdown drain and the health resource), share the policy, kind catalog, memory and progress stores with the extension engine, block the sensitive tools on the human approval gate rendered in the sidepanel, audit every call with the caller, tool and outcome, emit the mcp bundle through the build with size accounting and the naming check, add the mcp ci steps to the verify workflow and the containerfile smoke, and document the server, the transports, the catalog, the resources and the prompts in docs/16.mcpserver.md with the client examples.
- [x] 1.1.85: implement the native host bridge with the nativehost module (the host manifest template with the generated extension id placeholder, the installer behind the install consent and the uninstaller with injected filesystem seams, the runtime port with the crash and reattach path, the companion handshake with the one major version upgrade window, the capability negotiation, the correlation ids, the heartbeat liveness, the frame checks, the secret exclusion, the structured errors, the rate caps, the audit records and the diagnostics), the wsbridge localhost relay (loopback only binds, per session tokens with hash-only records, the frame authentication, the one extension connection rule, the idle expiry and the servercontract envelope translation), the companionbin recipe that builds from source with plain node, the per class and per surface consent gates with the sensitive human approval, the kill switch and the escape hatch, and the optional native messaging permission the manifest validator allows in the optional set only.
- [x] 1.1.85: wire the native command through the background and the cli (attach, detach, call, heartbeat, diagnostics, the install consent, the native install, uninstall and diagnostics subcommands), persist the install state and the call records through the memory seam across service worker restarts, render the nativehost settings panel with the install state, the versions and the diagnostics, emit the wsbridge and companion bundles through the build with size accounting, the naming check and the plain node recipe assertion, add the native smoke steps to the verify workflow and the containerfile against the fake host process with the host manifest install and removal, attach the host manifest template as a release asset, allow the new bundles through the npmgate, and document the install, consent and diagnostics walkthrough, the wsbridge protocol and token model and the companionbin build recipe in docs/17.nativebridge.md.
- [x] Build an audit timeline with traceable statuses and non-destructive diagnostics.
- [x] Build a security-policy interface for site scopes, permission limits, revocation, and stop controls.
- [x] Build a language assistant interface that drafts a reviewable automation plan without initiating execution.
- [x] Create a reusable browser-agent library with typed contracts for observation, tool execution, cancellation, failures, checkpoints, and browser adapters.
- [x] Create a Chromium extension connector with explicit pairing, minimal permissions, visible activity status, and non-destructive observation actions.
- [x] Create extension build, packaging, permission-validation, and GitHub Actions workflow configuration.
- [x] Document the evidence matrix, public sources, architecture, threat model, browser permissions, configuration, and release gates.
- [x] Update the README with neutral server URL configuration and license review requirements.
- [x] Add automated tests for permission gates, consent flows, plan approval, cancellation, policy revocation, and typed contracts.
- [x] Run type checking, unit tests, extension build, package inspection, and visual responsive review.
- [x] Prepare publication review materials without publishing the application, extension, or library.
- [x] Read the newly supplied extension requirements one attachment at a time and extract the authoritative scope.
- [x] Research at least 50 public browser-agent and browser-automation extensions, plus primary Chromium and Firefox extension documentation.
- [x] Build a source-backed feature and execution-flow catalog that distinguishes public claims, open-source implementation evidence, and unverified capabilities.
- [x] Obtain only publicly available extension artifacts or openly licensed source for interoperability and security analysis; do not reuse third-party code or branding.
- [x] Document per-artifact files, manifest permissions, message flows, local versus remote execution boundaries, and licensing where available.
- [x] Create a per-artifact comparison matrix covering message transport, local-versus-remote boundary, and source or license availability for all reviewed IDs.
- [x] Inspect a representative subset of public background, content, or popup entries to verify message-flow patterns without retaining or reusing third-party code.
- [x] Design a clean-room Devthink 1.1.1 architecture as a library-first browser-agent extension with configurable endpoints and no hard-coded service URLs.
- [x] Replace the current application-first layout with the versioned extension package, shared library, CLI/build tooling, tests, and documentation required by the new architecture.
- [x] Implement consent-first browser assistance features: connection prompt, active-tab observation, scoped permissions, agent tool bridge, diagnostics, cancellation, and audit events.
- [x] Add workflows for reproducible extension builds, artifact packaging, manifest checks, permission checks, and unit tests without automatic publication.
- [x] Validate the complete extension artifact and library package, then update all release documentation for version 1.1.1.
- [x] Generate and package the declared TypeScript definitions for the reusable library.
- [x] Add a structured local diagnostics capture and visible review flow to the extension.
- [x] Produce a reproducible ZIP artifact of the unpacked extension and validate its manifest and required files.
- [x] Re-run full validation and release-gate documentation after declaration, diagnostics and packaged-artifact checks pass.
- [x] Commit the completed version 1.1.1 to wenathlan/extension and push it to the configured GitHub repository.
- [x] Amend the 1.1.1 commit author to the authenticated wenathlan GitHub identity and verify it on the remote branch.
- [x] Document the distinction between the wenathlan commit-author metadata and the delegated authenticated GitHub account used to push the commit.
- [x] Read every root file and every workflow in wenathlan/saddle, wenathlan/devthink, wenathlan/e2ugh and wenathlan/maene.
- [x] Compare reusable workflow capabilities, metadata conventions, release triggers, artifact retention, digest generation and publication targets.
- [x] Design a single extension release workflow that derives version and release notes from package metadata and changelog entries.
- [x] Adapt build, test, ZIP artifact, digest, GitHub release, npm and GitHub Packages publication controls to this package without copying unrelated workflows.
- [x] Document registry compatibility and explicit credential or publisher prerequisites for npm, GitHub Packages, Maven and container publication.
- [x] Validate workflow syntax, package metadata and dry-run release behavior without publishing a package or creating a public release.
- [x] Add executable YAML and GitHub Actions workflow syntax linting, then rerun the release validation.
- [x] Commit and push the author correction and the workflow adaptation to wenathlan/extension.
- [x] Reauditar integralmente os workflows dos quatro repositórios autorizados e mapear responsabilidades reutilizáveis para a extensão.
- [x] Consolidar workflows por domínio correlato, eliminando separação redundante entre metadados, release e publicação.
- [x] Criar atualização controlada de dependências, engines, actions e metadados, com pull request revisável e bloqueio explícito de atualizações major não autorizadas.
- [x] Revalidar os destinos de publicação solicitados com evidência real de empacotamento e habilitar apenas formatos tecnicamente compatíveis com o pacote.
- [x] Validar localmente e no GitHub Actions, documentar o resultado e enviar a consolidação com autoria Nathlan Bros.
- [x] Confirmar que a atualização de dependências e actions preserva majors sem permissão e criar o caminho manual de autorização quando necessário.
- [x] Testar localmente os pacotes Maven e NuGet da extensão, ou restringir a publicação ao conjunto que tenha evidência verificável.
- [x] Atualizar a versão canônica do pacote para 1.1.2 e gerar os metadados derivados correspondentes.
- [x] Fazer o workflow de manutenção sincronizar automaticamente todos os metadados derivados após mudanças em package.json ou CHANGELOG.md.
- [x] Garantir que mudanças somente no CHANGELOG gerem release notes da versão canônica sem modificar versões indevidamente.
- [x] Validar local e remotamente a sincronização automática e enviar a alteração com autoria Nathlan Bros.
- [x] Inspecionar todos os alertas abertos de code scanning, dependências e workflows que não dispararam.
- [x] Atualizar ações e dependências compatíveis para versões correntes e corrigir os achados de segurança verificáveis.
- [x] Promover o pacote corretivo 1.1.6, gerar metadados e release notes isoladas e preparar os artefatos de release.
- [x] Validar o disparo de maintenance, verify, security e release em condições seguras.
- [x] Inventariar branches e pull requests abertos antes de qualquer fechamento ou arquivamento; no inventário final não restou branch não principal nem pull request aberto.
- [x] Corrigir as falhas de GitHub Release, GitHub Packages npm e GHCR observadas no release v1.1.3 sem mover ou substituir a tag publicada.
- [x] Promover uma versão corretiva com release notes, artefatos e publicações validadas após os reparos do pipeline.
- [x] Incluir a ferramenta unzip na imagem GHCR para validar internamente o ZIP da extensão durante o build de release.
- [x] Sincronizar todas as referências versionadas do README com a versão canônica e incluí-lo na verificação automática de metadados.
- [x] Auditar os identificadores atuais de npm, Maven, NuGet e GHCR e documentar os limites de nomenclatura de cada registry.
- [x] Promover o pacote para 1.1.8 e simplificar os identificadores NuGet e Maven para extension quando o formato do registry permitir.
- [x] Ajustar o release consolidado para consumir com segurança NODE_AUTH_TOKEN e NPM_TOKEN organizacionais na publicação npmjs.
- [x] Validar local e remotamente os artefatos e nomes da versão 1.1.8; a publicação por tag foi exercitada na versão corretiva 1.1.9.
- [x] Reauditar todos os arquivos de raiz e workflows por referências ao identificador NuGet anterior.
- [x] Promover a versão para 1.1.9 e garantir que o NuGet distribua somente o identificador extension.
- [x] Validar o pacote NuGet gerado, os metadados derivados e o GitHub Actions antes de publicar novo release.
- [x] Adicionar uma verificação estática dedicada para o fallback `NODE_AUTH_TOKEN || NPM_TOKEN` e documentar que npmjs só executa quando `PUBLISH_NPM=true`.
- [x] Reauditar todos os workflows dos repositórios autorizados e o estado atual de release, pacotes e artefatos de Devthink.
- [x] Promover Devthink para 1.1.10 e fazer package.json/CHANGELOG dispararem integralmente as automações compatíveis.
- [x] Adaptar atualização revisável de dependências, actions, engines e limpeza segura de cache sem executar ações destrutivas sem salvaguardas.
- [x] Completar os ativos de release para cada formato tecnicamente produzido e documentar os limites de anexação de registries.
- [x] Registrar que a publicação npmjs de 1.1.10 falhou por caminho de tarball e que a publicação automática com token foi comprovada nas versões corretivas 1.1.11 a 1.1.13.
- [x] Preparar a migração do pacote NuGet anterior e solicitar confirmação específica antes de excluí-lo ou ocultá-lo.
- [x] Remover o gate manual PUBLISH_NPM e publicar npmjs automaticamente somente após validação e autenticação de token bem-sucedidas.
- [x] Corrigir a resolução explícita dos tarballs locais nos destinos npm para que o npm não interprete os artefatos de release como repositórios Git.
- [x] Promover uma versão corretiva após a falha parcial de publicação 1.1.10, preservando a tag já criada e seus artefatos rastreáveis.
- [x] Executar a migração única e idempotente do pacote NuGet legado Wenathlan.Devthink.Extension, sem tocar no pacote extension.
- [x] Sincronizar a documentação de segurança, release e nomes de pacote com as evidências de v1.1.11 e a migração solicitada.
- [x] Corrigir o manifesto SHA256SUMS para que os nomes listados coincidam com os ativos baixados da GitHub Release.
- [x] Remover do workflow de release a etapa única de migração NuGet já concluída, preservando as demais publicações automáticas.
- [x] Reorganizar a documentação plana na raiz de docs, mover todo.md para docs e remover as subpastas docs/sources e docs/plans após conferir as referências.
- [x] Pesquisar fontes públicas e criar um catálogo rastreável de recursos, permissões e fluxos das extensões de referência, sem reutilizar código ou marcas de terceiros.
- [x] Atualizar a automação de manutenção para conferir fontes oficiais de versões de dependências, actions, Node, npm, pnpm e Bun, mantendo revisão humana para mudanças incompatíveis.
- [x] Declarar e verificar as compatibilidades de runtime Node, npm e Bun sem afirmar suporte não testado.
- [x] Corrigir a imagem de release Node 26 sem Corepack para instalar npm e pnpm pelas versões declaradas e publicar um patch automático sem reescrever v1.1.14.
- [x] Remover o atalho todo.md da raiz e manter o checklist exclusivamente em docs/todo.md.
- [x] Simplificar a distribuição NuGet para usar somente os arquivos necessários na raiz, removendo a pasta nuget sem alterar o identificador extension.
- [x] Expandir o inventário de extensões públicas por artefatos CRX baixados passivamente, registrando manifests, arquivos, permissões, metadados e fluxos sem executar nem reutilizar código de terceiros.
- [x] Definir e implementar apenas capacidades consent-first, de menor privilégio e testáveis que sejam priorizadas pelas evidências do catálogo.
- [x] Criar testes automatizados agrupados por contexto e executar um smoke test da extensão em perfil Chromium isolado, sem credenciais ou automação de ações sensíveis.
- [x] Validar a release automática 1.1.16 e registrar evidências de registries, artefatos e segurança; a origem e a tag foram sincronizadas, mas o verify falhou somente no smoke Chromium e bloqueou as publicações.
- [x] Corrigir a inicialização do smoke test Chromium em CI sem remover o isolamento do perfil nem enfraquecer a verificação da extensão empacotada; o v1.1.30 usa Chrome for Testing oficial e passou remotamente.
- [x] Executar a tentativa corretiva v1.1.18; ela preservou o isolamento, mas falhou antes de publicação porque o teste inferiu um arquivo de preferências que o Chrome ainda não havia criado.
- [x] Executar a tentativa corretiva v1.1.19; ela permitiu somente Devthink, mas o Chrome headless hospedado não expôs o worker MV3 e bloqueou a publicação.
- [x] Executar a tentativa corretiva v1.1.20; o display virtual iniciou corretamente, mas o Chrome hospedado continuou sem registrar um worker da extensão antes de publicação.
- [x] Executar a tentativa corretiva v1.1.22; ela isolou a página local e o worker esperado, mas o Chrome hospedado não os expôs antes de publicação.
- [x] Executar a tentativa corretiva v1.1.23; os diagnósticos mostraram que nenhum alvo DevTools ficou disponível dentro da janela de dez segundos no Chrome hospedado.
- [x] Executar a tentativa corretiva v1.1.24; a janela de trinta segundos preservou o isolamento, mas não obteve os alvos DevTools no Chrome hospedado e bloqueou a publicação.
- [x] Executar a tentativa corretiva v1.1.25; ela confirmou o popup local no runner, mas evidenciou que o worker MV3 não permanecia ativo antes de uma mensagem ao runtime.
- [x] Executar a tentativa corretiva v1.1.26; ela confirmou que a resposta CDP de contexto era indefinida no runner, sem acessar dados externos ou do usuário.
- [x] Executar a tentativa corretiva v1.1.27; o diagnóstico identificou uma expressão CDP com template literal incompleto, sem alterar o escopo isolado do teste.
- [x] Executar a tentativa corretiva v1.1.28; ela provou que o contexto DevTools do popup não expõe `chrome.runtime`, revelando que a exigência de worker ocioso era um falso negativo de ciclo de vida MV3.
- [x] Executar a tentativa corretiva v1.1.29; ela identificou que o Google Chrome hospedado bloqueia o carregamento unpacked por linha de comando com `ERR_BLOCKED_BY_CLIENT`.
- [x] Publicar e verificar v1.1.30 com o Chrome for Testing oficial, perfil isolado, registro Devthink e popup realmente renderizado; verify, security e a release completa concluíram com sucesso, há dez ativos, os nove ativos distributáveis listados passaram em `sha256sum --check` e o arquivo de somas tem digest publicado pelo release.
- [x] Expandir o vocabulário de ações consent-first com cinco novos kinds revisados (scroll, select, hover, extract, wait), mantendo os gates de sessão, aprovação de plano, aba e origem e sem solicitar nenhuma permissão nova.
- [x] Adicionar pausa e retomada de sessão no popup, bloqueando execução e pré-visualização enquanto pausada e auditando ambas as transições.
- [x] Rastrear progresso por passo em arquivo próprio de lógicas correlatas (progress.ts), marcar passos concluídos no painel lateral e fechar o plano aprovado como completed após o último passo revisado.
- [x] Ampliar o limite de passos revisados por proposta de vinte para cinquenta e registrar opções limitadas de elementos select no snapshot para propostas válidas.
- [x] Expandir o catálogo passivo de evidências CRX de 58 para 67 IDs (Selenium IDE, Instant Data Scraper, Table Capture, Copyfish OCR, Session Buddy e mais quatro não servidas), com 62 manifestos verificados e indisponibilidades registradas honestamente.
- [x] Promover a versão 1.1.31, sincronizar todos os metadados derivados, validar typecheck, testes, build, empacotamento e workflows localmente antes do envio.
- [x] Mine the full CRX evidence corpus: download all 67 extension IDs from the Google update api, analyze every file, and map 2456 per-extension features into a 626-item consolidated pool (docs/12.crxfeaturemining.md).
- [x] Plan the complete evolution chain from 1.1.31 to 2.0.0 with exactly one hundred changes per release across 69 planned releases (docs/13.evolutionroadmap.md).
- [x] Remove every arbitrary bound: unbounded waits, unlimited plan size, configurable plan expiry, retention-driven audit and outcome history, and complete unbounded snapshots.
- [x] Grow the action vocabulary from eleven to sixty three kinds covering pointer, keyboard, drag and drop, upload, form control, page movement, reads, mutation, evaluation and browser-level command.
- [x] Introduce the optional capability model: tabs, downloads, clipboardRead and clipboardWrite as runtime-requested permissions with capability gates, panel grant flow and audited grants.
- [x] Add the reviewed options field to steps, structured details to step results, outcome history to progress, prior snapshots to plan resets, and a versioned observation schema.
- [x] Group correlated page logics into pageactions.ts, pagereads.ts and browsertabs.ts while pagebridge.ts keeps the dispatch seam.
- [x] Complete the interaction universe: grow the reviewed vocabulary from sixty three to ninety kinds with pointer paths, coordinate clicks, text, aria, name, xpath, index and point target resolution, timed typing, key holds, sliders, dates, colors, shadow dom piercing, iframe entry, dialog answering and retry rules.
- [x] Build the target resolution engine in pageresolve.ts with one pass targetref resolution, ambiguity candidate lists, resolvedtarget summaries, open shadow root piercing, same origin frame walking and the numbered clickable map builder.
- [x] Implement pointer path math in pagepointer.ts with waypoint interpolation, easing shapes, peak velocity caps, jitter windows, hop settling and full coordinate click sequences.
- [x] Keep key hold state across steps and service worker restarts through the persisted registry, audit press and release timestamps, and report held keys in the context payload and popup.
- [x] Answer confirm, alert and prompt dialogs per reviewed dialog policies installed in the main world through the scripting api, harvest the observed dialogs for the audit trail and arm a persistent auto handler for session tabs.
- [x] Wrap failed interactions in reviewed retry rules with no attempt ceiling, revalidating target geometry between attempts and recording attempts and movement deltas.
- [x] Store clickable maps by observation version, render the numbered map in the side panel and let the user pick an entry as the target hint for the next plan input.
- [x] Group the new page logics into pageresolve.ts, pagexpath.ts, pagepointer.ts, pageinteract.ts, pagecontrols.ts and pagedialogs.ts while pagebridge.ts keeps the dispatch seam.
- [x] Extend the vitest suite to 76 tests with the pagebridge family covering resolution, xpath, pointer math, control math, holds, dialogs, frame walking and retries on plain fixture shapes.
- [x] Document the target resolution grammar and the new interaction kinds in docs 03, the pointer paths, retry rules and numbered clickable map in docs 05, and promote 1.1.33 with synchronized metadata.
- [x] Deepen observation: grow the reviewed vocabulary from ninety to one hundred eighteen kinds with the accessibility tree, reader views, visible text, outlines, selections, open graph and embedded json state reads, all read only behind the same gates.
- [x] Build the observation walker in pageobserve.ts piercing open shadow roots and same origin iframes, scoring text density for the reader heuristic, resolving page language from document, meta and content signals and routing extracted text by detected language.
- [x] Detect page shapes in pagedetect.ts: repeated lists with shared item selectors, normalized table shapes, pagination estimates, infinite scroll ranges, virtualized lists, lazy images, sticky overlays, scroll locks, template classes and stable section fingerprints.
- [x] Add the watch vocabulary in pagewatch.ts: mutation, focus and banner watches over reviewed lifetime windows with batched records, the network quiet probe through the performance timeline, the snapshot diff engine hashing node summaries and the derived selector scorer ranking id, attribute, text and structural candidates by stability.
- [x] Require a reviewed lifetime window for every watch kind, keep the quietrule thresholds user configured with no code ceiling, and gate readjson payload inspection behind the session origin grants.
- [x] Keep watch registrations alive across service worker restarts in persisted storage with restart reconciliation, audit every registration with its reviewed window, dispatch mutation and focus events into the step result stream and count watch steps completed only once the lifetime window closes.
- [x] Store observation versions, a11y trees, reader articles, mutation, focus and banner streams, snapshot diffs, derived selectors and template profiles in memory accessors with user configured observation retention.
- [x] Render the observation surfaces: the a11y tree beside the dom snapshot, the reader view with heading blocks, detected lists, tables and pagination as plan suggestions, the live watch stream, snapshot diff rows, the consent banner review card, derived selectors with stability scores and network quiet progress in the side panel, plus page language, template and scroll lock signals in the popup.
- [x] Extend the vitest suite to 124 tests covering the observation kinds read only grading, watch lifetimes, quiet rules, diff versions, origin gated json reads, observation envelopes, event records, diff payloads, page signals and the stored evidence accessors on plain fixtures.
- [x] Document the observation pipeline and watch lifetimes in docs 03, the list, table and banner detection heuristics in docs 05, the observation kind flow map in docs 07, and promote 1.1.34 with synchronized metadata.
- [x] Master navigation: grow the reviewed vocabulary from one hundred eighteen to one hundred forty seven kinds with the container, page world, sequential and safety navigation families, all behind the same session, plan approval and origin gates.
- [x] Add the navigation parameter grammar: navtarget with url, container, position and private flag, waitprofile with load signals, thresholds and per origin overrides, and urlpattern with exact, prefix, host and pattern modes plus query and fragment parts.
- [x] Group the navigation logics into pagenav.ts (targets, containers, wait profiles, url patterns, link following, spa routing, query rewriting, deep links, recent tabs) and pagenet.ts (redirect chains, http and interstitial states, rate windows, safety verdicts, curated batches, prefetch verification, preconnect origins, auth gating) while pagebridge.ts keeps the dispatch seam.
- [x] Resolve navtarget containers across tabs, windows and private profiles in the background, keep wait profile overrides per origin in memory, watch tab updates to build redirect chains and detect spa route changes, keep the navigation trail per session with step refs, and remember recently closed tabs for reopentab.
- [x] Enforce navrate windows per domain from the reviewed policy with user configured windows and ceilings, refuse navigation away from a task tab outside the session grants, require checksafe verification before an unreviewed origin opens, and refuse the whole batchopen list when any url is unsafe.
- [x] Route printpdf output into the task artifact store, verify prefetch candidates against the session grants, apply the certificate interstitial policy by reporting instead of bypassing, and store basic auth credentials only after explicit review behind the consent gate.
- [x] Record each navlist entry as it completes in the plan progress, grow audit kinds with navigation, redirect, auth, prefetch and rate events, and add memory accessors for trails, wait profiles, navigation records, navintents, rate states, curated lists, auth records, artifacts, nav control, safeties, recent tabs and queue counts.
- [x] Render the navigation surfaces: the trail timeline, redirect chains and final urls on navigation steps, navlist progress with current url and remaining count, rate limit windows per domain, paused navigation highlighted while a consent prompt is open, curated link lists with per url safety states, the checksafe runner, the basic auth credential prompt in the side panel, plus load phase, offline state, active wait profile and queued target badge counts in the popup.
- [x] Extend the vitest suite to 160 tests covering the navigation grammar, sensitive and read only grading, rate window math, wait signal evaluation, url pattern matching, query rewriting, link matching, deep link building, redirect chain assembly from navigation watch fixtures, spa route classification, http and interstitial detection, safety verdicts, batch refusal, prefetch grant checks and auth credential gating on plain fixtures.
- [x] Document the navigation model and wait profiles in docs 03, the spa routing, rate limits and trail audit in docs 05, the navigation kind flow map in docs 07, and promote 1.1.35 with synchronized metadata.
- [x] Command tabs and windows: grow the reviewed vocabulary from one hundred forty seven to one hundred seventy nine kinds with the tab query, tab mutation, group, layout, window command, routing, snapshot and watch families, all behind the same session, plan approval and origin gates plus the optional tabs capability.
- [x] Add the tab and window parameter grammar: tabquery with url, title, id and pattern matchers (at least one matcher, `*` and `**` wildcards), tabgroupspec with name, color, member tabs and collapse state, tablayout with name, tabs, groups, positions and window bounds, and windowstate with bounds, maximize state and profile kind.
- [x] Group the tab command logics into tabscommand.ts (query matching, clone detection, search and audio enumeration, layout capture and restore planning, group membership, close selection with session tab protection, discard candidates, zoom and switch math, switcher ordering, badges, task tab gauge, grant inheritance profiles) while pagebridge.ts keeps the dispatch seam.
- [x] Grade closepattern, discardtab, incognitowindow and scratchwindow as sensitive kinds with every other tab and window mutation, keep enumeration and watching read only, require the explicit reviewed flag before any closepattern closes, and always refuse to close the session tab itself.
- [x] Require explicit review before closing any window that holds more than one task tab, restrict group and layout mutations to the active session, separate incognito windows from session grant inheritance, and keep the concurrent task tab ceiling a user configured choice with no hardcoded cap.
- [x] Keep tabgroup membership through moves and renames in the Devthink group registry (the manifest deliberately excludes the tabGroups permission), keep named layouts with their window bounds and group states, watch tab title, activation and closure events into a persisted stream, update badges from the live progress state of each task, and restore discarded tabs on demand without losing their urls.
- [x] Add memory accessors for named layouts with timestamps, tab group definitions with color choices, tabmeta records with task provenance, session snapshots, the closed tab history, badge states per task, the watchtab event stream, scratch window ids and the pinned control tab state; grow audit kinds with group, layout, discard and badge events; grow progress with task tab assignment and release.
- [x] Extend tabcreate with reviewed background and window options, windowcreate with reviewed left, top, width, height and state options, and document the tabquery, group and layout grammars plus the tabreport and layout envelopes in the protocol.
- [x] Render the tabs and windows surfaces: the quick switcher by recency with filter keys and jump actions, searchtabs results with jump to tab, groups with colors and collapse states, per tab badges and audio marks, duplicate tab warnings, layout save and restore, the session snapshot card with restore actions, the task tab budget gauge, the window close prompt and the pinned control tab feed in the side panel, plus the window layout with quick state buttons and the task tab badge count in the popup.
- [x] Extend the vitest suite to 195 tests covering the tabquery grammar and matching, clone detection, group membership across moves and renames, layout save and restore with window bounds, watchtab event dispatch, discard and restore of inactive tabs, incognito separation, badge updates from progress, snapshotsession capture and reopenrun restore, the tabcreate and windowcreate options, closepattern consent refusal paths, sensitive and read grading, the window close gate, the layout session gate, every new memory accessor and the task tab budget as a user choice.
- [x] Document the tabs and windows command model in docs 03, the tab watchers, layouts and session snapshots in docs 05, the tabs and windows kind flow map in docs 07, the tabgroups and incognito capability requirements in docs 01, and promote 1.1.36 with synchronized metadata.
- [x] Fill forms with review: grow the reviewed vocabulary from one hundred seventy nine to two hundred one kinds with the forms and data part one family — thirteen sensitive fill, submit, wizard and payment kinds plus nine read only inspection kinds, all behind the same session, plan approval and origin gates with no new permission.
- [x] Add the form parameter grammar: formrecord with optional form selector and entries of match, kind and value; fieldmatch with label, placeholder, arialabel and name modes; valuegen with field kind, locale and seed; formprofile with name, fields and origin grants; submitticket with form ref, values hash and consent ref.
- [x] Require an asksubmit review step before every submitform and retryform (the proposal parser refuses the plan and canexecute rechecks the gate), require the explicit consentpassword kind with a reviewed consent ref for any password fill while password entries are refused inside form records and profiles, and refuse generated values that look like real card numbers or personal identifiers.
- [x] Group the form logics by context: pageforms.ts holds the field matcher, classifier, seeded locale aware generator, values hash, fill operations with honeypot skips, card mask, honeypot, login, template and captcha detectors and the error associator; pagewizards.ts holds the wizard tracker, dependent load wait, typeahead pick, calendar plan, card groups, code source gate and retry backoff math; pagebridge.ts keeps the dispatch seam.
- [x] Keep the consent heavy work in the background: saveprofiles stores profiles behind origin grants, asksubmit records the submission ticket with its values hash and opens the sidepanel prompt, submitform verifies the approved consent ref before the owning form submits, retryform enforces the reviewed backoff windows with no code ceiling, consentpassword audits the ref without the value, attachfile resolves run store artifacts, and handoffcaptcha pauses the plan until the user resolves the handoff.
- [x] Add memory accessors for form profiles with origin grants and timestamps, wizard states with step history, submission tickets with values hashes, error reports, typeahead picks, captcha handoffs with resolution state, login and template detections per origin and the consent gated one time code; grow audit kinds with fill, submit, consent and handoff events; track wizard completion in progress as executed steps over total steps.
- [x] Document the formrecord, fieldmatch and valuegen grammars in the protocol and add the formreport, errorreport and wizardreport envelopes with wizard states and typeahead picks in the session context.
- [x] Render the forms and data surfaces: the form map with matched fields, kinds and honeypot skips, generated values with regenerate buttons per field, saved profiles with grants and apply/remove actions, asksubmit cards with the full values diff, wizard step indicators, inline error reports with field refs, template badges, masked card segments, the consent gated one time code entry and the captcha handoff card in the side panel, plus the captcha handoff state in the popup and the open asksubmit prompts in the badge count.
- [x] Extend the vitest suite to 215 tests covering sensitive and read grading, the asksubmit review gate at parse and execution time, the password consent gate, generated value refusal, the full form grammar, field matching modes, kind classification, seeded locale aware generation, fill operations with honeypot skips and ambiguity, error association, backoff windows, wizard advancement, dependent loads, typeahead picks, calendar plans, artifact attachments, card groups and masking, code sources, honeypots, login and template detection, captcha handoff, every new memory accessor and wizard progress on plain fixtures.
- [x] Document the forms and data model and consent gates in docs 03, the wizards, dependent controls and retry loops in docs 05, the forms kind flow map in docs 07, the profile storage and password consent in docs 01, and promote 1.1.37 with synchronized metadata.
- [x] Complete form mastery with parsing and persistence: grow the reviewed vocabulary from two hundred one to two hundred eighteen kinds with the forms and data part two family — eight sensitive export, streaming and pagination kinds plus nine read only extraction, transform, merge and provenance kinds — all behind the same session, plan approval and origin gates with no new permission.
- [x] Add the dataset parameter grammar: dataset with column specs, keyed rows and source refs; columnspec with key, label, kind and normalized name; transformrule with expression, source columns and target column; extractsession with pages visited, row counts and resume cursor; provenancerecord with url, timestamp, step ref, row range and checksum.
- [x] Build the table reader in pagedata.ts: header normalizer into stable slugified keys, rowspan and colspan expansion into filled grids, nested table extraction into child datasets linked to parent rows, pagination follower that clicks next controls and waits for fresh rows, row hasher, transform engine with per rule error surfacing, merge engine with column alignment and gap filling, row sampler, and csv, json and excel (SpreadsheetML) serializers with a quoted-field csv parser and column mapping.
- [x] Build the dataset command logics in datacommand.ts: dataset records, artifact exports with deterministic checksums, chunk planning with backpressure, stream state advancement and resume, extraction cursor advancement, loop row variables with {{column}} interpolation, provenance records with row ranges, artifact retention and sheet payload shaping; pagebridge.ts keeps the dispatch seam.
- [x] Grade every export kind sensitive (exportcsv, exportjson, exportexcel, copytable, pushsheets, streamdisk) and refuse any export while the session origin grants do not cover the active origin; require the explicit reviewed flag plus a configured HTTPS endpoint behind its optional origin permission for pushsheets, and the optional clipboardWrite capability for copytable; keep row limits, page counts, sample sizes and chunk sizes user configured with no code ceilings.
- [x] Keep the consent heavy work in the background: executors write csv, json and excel artifacts into the task artifact store with provenance records per export, stream chunks to disk with backpressure while the stream state persists for resume across service worker restarts, keep extractsession cursors so resumeextract continues interrupted extractions, expose dataset rows to looprows as step variables for fill loops, parse imported csv with column mappings, deduplicate by reviewed keys, merge page datasets, stamp rows with source refs and preview grids before export.
- [x] Add memory accessors for datasets by id with column specs and row counts, imported csv datasets, extract session cursors and page histories, stream chunk states, provenance records per exported artifact, transform rules and dedupe keys per task, sheet endpoint configs and exported artifacts under the user configured artifact retention; grow audit kinds with scrape, export, stream, resume and provenance events; track extraction progress as rows collected over the estimated total.
- [x] Document the dataset, transformrule and extractsession grammars in the protocol and add the dataset envelope with column specs and sampled rows, the extraction progress report and the provenance report per exported artifact.
- [x] Render the dataset surfaces: the sortable preview grid, extraction progress cards with resume prompts for interrupted extractions, per dataset export actions with format choices, transform rules with per column previews, dedupe results with removed counts, provenance records per artifact with sources, the csv import picker with column mapping, sheet endpoints with grant states and per iteration loop variables on the step timeline in the side panel, plus the active dataset row count and stream state in the popup and the dataset count in the action badge.
- [x] Extend the vitest suite to 243 tests covering header normalization, span expansion, nested table extraction, pagination following with row freshness waits, row hashing and dedupe removal counts, transform expressions and error surfacing, merge alignment, row sampling, csv round trips with quoted fields, column mapping, artifact checksums and row ranges, chunked streaming with backpressure, cursor resume after a simulated restart, loop variable interpolation, retention, the export grading and origin gates, the full data grammar, the dataset, extraction and provenance envelopes, every new memory accessor and the extraction progress share on plain fixtures.
- [x] Document the extraction pipeline and artifact store with streaming and resume guarantees in docs 03, the pagination, dedupe, merge and resume strategies in docs 05, the datasets and exports kind flow map in docs 07, the sheet endpoints and artifact exports in docs 01, and promote 1.1.38 with synchronized metadata.
- [x] Manage files, clipboard and downloads with review: grow the reviewed vocabulary from two hundred eighteen to two hundred thirty one kinds with the files, clipboard and downloads family — ten sensitive batch queue, interception, clipboard, quarantine, scan and cleanup kinds plus three read only verification, netlog and naming kinds — all behind the same session, plan approval and origin gates with the manifest permissions unchanged.
- [x] Add the files parameter grammar: downloadspec with urls, filename rule and completion criterion; downloadrecord with per file states, browser download id, resolved path, bytes and checksum; mimefilter with include and exclude patterns and the deny default for unlisted mime types; netlogrecord with url, method, status, timing and step correlation through request ids; clipentry with kind, payload hash and origin provenance; scanverdict values pending, clean, flagged and error; quarantineentry with path, reason, scan state and release ref; capturename with task, step and sequence parts; cleanuprule with age, kind and keep policies.
- [x] Group the files logics into filescommand.ts (queue transition table, concurrent window as a user choice, filename conflict suffixes, verification matching, mime matching with the deny default, header redaction, netlog correlation, clipboard entries with hashes, quarantine paths and scan verdict mapping, capture naming counters, cleanup sweeps with kept references) while pagebridge.ts keeps the dispatch seam and reports clipboard write results with payload hashes.
- [x] Keep the consent heavy work in the background: batchdownload starts waves under the user configured concurrent window and persists every per file state, pausedownload and resumedownload walk the queue through the downloads api, verifydownload compares state, size and checksum, interceptmime arms filters only inside the session origin grants and never for extension downloads, readclipboard needs an approved single use consent prompt and never persists the payload text, writeclipboard and copyscreen carry payload hashes, quarantinedownload and scanvirus gate releases on clean verdicts with hook failures pending, namecaptures stamps per task counters and cleanupartifacts sweeps by reviewed rules while keeping open review references.
- [x] Add memory accessors for download records, netlog records under the user configured netlog retention, clipboard consent records with single use approval, clip entries, quarantine entries, cleanup rule sets and run history, capture naming counters per task, artifact inventories, scan hooks, armed mime filters and artifact removal for the sweeper; grow audit kinds with intercept, clipboard, quarantine and cleanup events; track batch downloads in progress as files completed over total.
- [x] Document the files grammars in the protocol and add the downloadreport envelope with states, paths and checksums, the netlog report with step correlation and the quarantine report with scan verdicts and release refs.
- [x] Render the files surfaces: the batch download queue with per file states and pause, resume and verify actions, the armed mime interception rules with origin grants, clipboard consent prompts with the requesting step, the netlog viewer with step correlation and the redaction notice, the quarantine list with scan verdicts and release actions, capture naming previews, cleanup policy editing, the artifact inventory with sizes and ages, copyscreen results and the scan hook configuration in the side panel, plus live download states, quarantined files awaiting scan verdicts in the badge and the clipboard consent state while a read waits in the popup.
- [x] Extend the vitest suite to 268 tests covering the queue transition table with impossible transitions, the concurrent window as a user choice, filename conflict suffixes, verification matching, mime filter matching with the deny default, netlog correlation and header redaction, clipboard entry hashing without payloads, quarantine release only after clean verdicts, scan hook failure mapping, capture naming counters, cleanup sweeps by age and kind with kept references, the files grammar with consent and origin gates, the download, netlog and quarantine envelopes, every new memory accessor with netlog retention and artifact removal, and the batch download progress share on plain fixtures.
- [x] Document the files, clipboard and quarantine model in docs 03, the batch queues, scan hooks and cleanup sweeps in docs 05, the files kind flow map in docs 07, the downloads, clipboard and scanning capability requirements in docs 01, and promote 1.1.39 with synchronized metadata, closing phase one of the unlimited agentic core.
- [x] Give the agent eyes with review: grow the reviewed vocabulary from two hundred thirty one to two hundred thirty six kinds with the media capture family — five read only viewport, full page, element, region and contact sheet kinds — all behind the session, plan approval, active tab grant and origin gates with the manifest permissions unchanged.
- [x] Add the capture parameter grammar: captureoptions with format (png, jpeg, webp), quality in the zero to one hundred range, pixel ratio from one up with no code ceiling, an annotate flag and an export target of memory, download or clipboard; regionrect with x, y, width and height in css pixels plus the explicit reviewed flag; stitchplan with tile grid, scroll offsets and overlap rows; sheetlayout with cell size, column count and label style; capturepolicy modes off, manual, annotated and beforeafter; capturenaming with run, step, sequence and kind segments; shotrecord and shotpair with run, step, action and dom snapshot linkage.
- [x] Group the capture logics in the root capture.ts module (options defaults, stitch tiling, linear seam weights and band blending, fixed header match, pixel ratio rect scaling, element crop with the viewport crossing fallback, container region steps, contact sheet grid, annotation plan, naming rule builder, state pairing) while pagebridge.ts keeps the dispatch seam plus the measurepage, elementrect, preparecapture, scrollcapture, restorecapture, scrollcontainercapture and waitsettle measurement seams.
- [x] Keep the consent heavy work in the background: shotview shoots the visible viewport in the reviewed format and quality, shotfullpage scrolls in reviewed tile steps with the settle window and composes tiles on an offscreencanvas with overlap blending, fixed header skipping, hidden capture scrollbars and restored scroll position while the stitch budget stays inside the reviewed wait window, shotelement crops the scaled rect with the tiled fallback, shotregion walks scrollable containers in reviewed steps, contactsheet tiles labeled element captures, beforeafter wraps page moving actions with before and after shots linked to the dom snapshot id, and export routing negotiates clipboardwrite and the reviewed download flow through the permissions api.
- [x] Gate every capture kind on the active tab grant of the live session through the new capturegate, require the explicit reviewed flag on every regionrect before shotregion runs, refuse formats outside the png, jpeg and webp set, keep quality and pixel ratio user choices with no code caps, route memory, clipboard and download exports through their consent layers, and keep capture retention and contact sheet cell counts user configured windows with no hard caps.
- [x] Add memory accessors for capture records with bytes and step linkage under the user configured capture retention window (bytes expire, metadata survives), getcapture, listcaptures filtered by run, step and kind, and shotpairs per run; grow audit kinds with capture events carrying kind, target and export target; track capture completions and pair ids in progress as reviewable evidence.
- [x] Document the capture grammars and evidence in the protocol (capture options per kind, the capture block with record id, format and byte size in the outcome envelope, the capturereport envelope with records and pairs, the capture kinds listed among the available capabilities of every proposal request) and render the capture gallery in the sidepanel (thumbnails per run, full size opening with metadata, download and clipboard copy, shotpairs side by side with a divider, clickable contact sheet cells, stitch progress, the capture policy toggle, annotated evidence marked distinctly) plus the capture count and policy in the popup and the action badge.
- [x] Extend the vitest suite to 285 tests covering the options payload validation of every capture kind, the capturegate with its active tab grant and origin grants, format, quality and retention validation, stitch tiling with repeating fixed headers, overlap blending math across tile seams, element rect scaling at pixel ratios one, two and three, region capture of a scrollable container, contact sheet grid layout and cell labels, capturestates pairing around a click action with the failed action skip, buildname uniqueness across a long run, the capture block and options grammar in the protocol, and capture storage with listcaptures and retention expiry on plain fixtures.
- [x] Document the capture pipeline in docs 03, the stitching, blending and state pairing algorithms in docs 05, the media capture kind flow map in docs 07, the page capture permission boundary in docs 01, and promote 1.1.40 with synchronized metadata, opening phase two of the unlimited agentic core.
- [x] Record and transform media under review: grow the reviewed vocabulary from two hundred thirty six to two hundred forty eight kinds with the media capture part two family — three sensitive kinds (recordscreen, captureaudio, downloadimages) plus nine read only document, frame, canvas, stream, asset, lapse and transform kinds — all behind the session, plan approval, active tab grant (mediagate) and origin gates with the manifest permissions unchanged and the pinned key untouched.
- [x] Add the media parameter grammar: pdfoptions with paper inches, margins, scale, landscape and paginate plus break point selectors and a memory or download export route; recordingoptions with tab or run scope, fps, bitrate and the audio flag behind an approved recording consent prompt consumed per start and a duration window from the reviewed options or the user configured recording window; imagefilter with selector scope, minimum dimensions and a format list; framerecord with source, reviewed timestamp and poster flag; lapseplan with interval, duration and format kept inside the reviewed wait budget; convertdirective with source, target and quality inside the png, jpeg and webp set; thumbdirective with positive size, cover or contain fit and a naming suffix with no fixed size set.
- [x] Group the media logics in the root media.ts module (pdf option and page size resolution with the landscape swap, report segment splitter at reviewed break points, text layout wrapper, derived PDF 1.4 composer with xref offsets and escaped text, recording records with frame intervals and clean stops, image filter matching with url deduplication and counter names, lapse frame ordering, conversion and thumbnail directives with the cover and contain draw geometry, raw media, asset and stream normalization) while pagebridge.ts keeps the dispatch seam plus the pdfsegment, pdfbreaks, videoframe, canvasdata, streamelements, mediaelements, pageassets and pageimages seams.
- [x] Keep the consent heavy work in the background: capturepdf composes derived pdf reports from page segments with buildname naming routed to memory or the reviewed download flow, recordscreen and captureaudio open a recording consent prompt per start, record inside the user configured window, stop cleanly at run end and store honest JSON manifests through the reviewed download flow (tabCapture and offscreen stay outside the manifest, so recordings derive frame sequences and audio element evidence instead of encoded bytes), captureframe seeks and grabs video frames, downloadimages matches the reviewed filter, deduplicates urls and downloads through the reviewed flow behind the optional downloads capability, shotcanvas reads 2d and webgl buffers, probestream reports track details read only, readmedia and readassets extract sources and assets, timelapse captures ordered lapse frames inside the wait budget, and convertimage with makethumbs re-encode stored captures as derived records also offered as review panel actions.
- [x] Add memory accessors for the media store (addmedia, getmediarecords, listmedia filtered by run and kind, getmediarecord, getrecording with its file reference, removemedia) under the user configured media retention window where bytes expire while the metadata and recording frame index survive, plus image batches with match counts and recording consent decisions per origin; grow audit kinds with media events carrying kind, scope and byte size; track media completions in progress as reviewable evidence.
- [x] Document the media grammars and evidence in the protocol (media options per kind, the media block with record id, kind and byte size in the outcome envelope, the mediareport envelope with records and image batches, the media kinds listed among the available capabilities of every proposal request) and render the media tab in the sidepanel (pdf, image, video and audio sections; recordings with play, download and delete controls marked with a sensitive outline; image batches with filter match counts; stream probe results with track details; the recording indicator while recordscreen runs; lapse playback; convertimage and makethumbs actions on stored captures) plus the recording state badge in the popup and the media count in the action badge.
- [x] Extend the vitest suite to 306 tests covering the pdf options and pagination grammar with break point segments, the derived pdf document structure with header, xref offsets, landscape swap and text escaping, recording records with scope, duration and clean stops, image filter matching with url deduplication and counter names, lapse frame ordering with interval bounds, thumbnail geometry for cover and contain fits, the mediagate with recording consent and download grading, the media block and media report envelopes, media storage with retention expiry that keeps the recording index, and media progress evidence on plain fixtures.
- [x] Document the media pipeline and the honest recording host limits in docs 03, the pdf composition, recording and lapse math in docs 05, the media capture part two kind flow map in docs 07, the recording consent and derivation boundary in docs 01, and promote 1.1.41 with synchronized metadata, completing the media capture phase of the unlimited agentic core.
- [x] Implement the network observation part one roadmap of 1.1.42: fetchurl, parsejson, parsehtml, callrest and callgraphql as reviewed action kinds behind the session, plan approval and origin grants with the manifest permissions unchanged, the origincheck gate confining every outbound request to granted HTTPS origins, read only grading for fetch and parsing kinds and sensitive grading for the typed calls whose mutating verbs and credential bearing headers stay under explicit review.
- [x] Add the root httpclient.ts module grouping the correlated fetch logics (sendfetch with timeout races, backoff retries and the redirect follow limit, readstream chunking with the byte budget and abort flag, readpath dotted json extraction with kinds, defaults and miss flags, parsehtmlbody over a parse seam, payload schema validation with defaults, callrest with url templating and reviewed success classes, callgraphql with envelope wrapping and data and error unwrapping) exported through the library index, with pagebridge.ts keeping the parsehtmlmarkup domparser seam and background.ts keeping the executors, the in flight abort map and every audit.
- [x] Gate custom headers behind per origin fetch consents (credential bearing names always need the consent that names them, prompts appear once per origin and expire on the reviewed window, header values and body bytes never enter the audit trail), keep the reviewed fetch policy of timeout, retries, backoff and follow limit inside the reviewed wait budget with no code ceiling, stream large bodies chunk by chunk inside the user configured byte budget, and attach stored api key references only inside their origin scope when the plan reviews them.
- [x] Add memory accessors for the call store (addcall, getcalls, getcall, listcalls filtered by run and origin) under the user configured call body retention window where bodies expire while the metadata survives, typed endpoint definitions with version history, fetch consent decisions per origin and api key references with their secrets behind storage ids; grow the audit kinds with call events carrying method, origin and status class; track call completions and fetch retries in progress as reviewable evidence.
- [x] Document the http grammars and evidence in the protocol (fetch, rest and graphql options per kind, the transport block in the outcome envelope, the callsreport envelope with bodies held back, parseproposal rejecting ungranted origins and empty header names, the http kinds listed among the available capabilities) and render the network calls view in the sidepanel (status, duration, retries and byte counts per call; expanded header names, parsed fields and errors; header consent prompts with name and value; stream progress bars; origin, method and status class filters; endpoint listings; api key management; the call retention setting; the reviewed download flow export) plus the outbound call count in the popup and the credential call badge.
- [x] Extend the vitest suite to 335 tests covering sendfetch timeout, retry and backoff behavior, the redirect follow limit, readpath extraction with miss outcomes, html query selectors with counts, readstream chunking with byte budget aborts, callrest schema validation and url templating, callgraphql data and error unwrapping, the origincheck, header consent and sensitive grading gates, the transport block and calls report envelopes, call storage with consent expiry and body retention, endpoint version history and api key secrets, and call and fetch retry progress evidence, plus the isolated Chromium smoke test fetching a reviewed url through the shipped http client and parsing its json.
- [x] Document the http client and endpoint typing in docs 05, the network observation family in docs 03, the network observation part one kind flow map in docs 07, the honest opaque redirect note in the changelog, and promote 1.1.42 with synchronized metadata, opening the network observation phase of the unlimited agentic core.

- [x] Implement the network observation part two roadmap of 1.1.43: opensocket, sendmessage, waitmessage, watchrequests, readheaders, capturebodies, subscribesse, longpoll, mapapi and extractapi as reviewed action kinds behind the session, plan approval and origin grants with the manifest permissions unchanged, the socketgate confining every channel url to granted wss or https origins, the watchgate requiring the user granted webrequest toggle before request watching runs, read only grading for the observation kinds with sensitive grading for message sends, private mime body captures and mutating replays through the resolved risk of the reviewed options.
- [x] Add the root socketbus.ts module grouping the correlated channel logics (channel open options with the https origin mapping of wss urls, channel records, exponential reconnect waits capped at the user configured ceiling, the connect seam open, clean closes, message multiplexing with channel scoped sequences, filter matching by stream and dotted path with reviewed limits, sequence integrity, the server sent events parser with id, event, data and retry fields and the rest buffer, last event id resume headers, subscription options with cancellation paths, and the long poll cursor and decision math) and the root netwatch.ts module grouping the correlated watch logics (derived resource facts, failure classification, correlation ids, exchange records with honest question mark methods, request and response pairing, exchange filtering, header allowlists with required redaction lists, body filters with byte ceilings, private mime grading, body capture with truncation, page api grouping and ranking, and replay specs with overrides and extraction paths), both exported through the library index with pagebridge.ts keeping the resourcerecords seam and background.ts keeping the executors, the active socket map with clean closes at run end and on the killswitch, and every audit as socket and replay events without payload, header or body values.
- [x] Add memory accessors for the channel store, the message queue in arrival order with draining, the exchange store filtered by run, origin and status, captured bodies under the user configured body retention window where bodies expire while the exchange metadata survives, event subscriptions and page api maps per origin; grow the audit kinds with socket and replay events; track channel transitions, observed exchanges, event stream observations and poll iterations in progress as reviewable evidence.
- [x] Document the socket and watch grammars and evidence in the protocol (socket, message, filter, subscription, poll, watch, header filter, body filter, replay options per kind, the network block with exchange count and channel state in the outcome envelope, the exchangesreport envelope, parseproposal rejecting ungranted channel urls, missing cancellation paths, missing stop conditions and lifetimes beyond the plan window, the socket and watch kinds listed among the available capabilities) and render the network view in the sidepanel (method, url, status, size and duration per exchange; expanded redacted headers and body previews; error class badges; correlation id grouping; live channel state with message counters and close actions; event stream names and resume points; poll cursors and stop conditions; the webrequest grant toggle; the body retention setting; the netlog export) plus the observed request count and live channel count in the popup and the action badge.
- [x] Extend the vitest suite to 366 tests covering channel opens with reconnection backoff and failure classes, backoff ceiling growth, stream multiplexing with channel scoped sequence ordering and integrity, message filter matching with limits, server sent events parsing with resume headers, long poll cursor loops with stop conditions, poll ceilings, cancellation and plan expiry, derived request facts with failure classification, exchange pairing by correlation id, header allowlists with redaction, body filters with byte ceilings and truncation, page api grouping and ranking, replay specs with overrides and extraction paths, the socketgate, watchgate and observed origin gates, the socket and watch grammars, conditional capturebodies and extractapi risk resolution, the network block and exchanges report envelopes, parseproposal rejections, channel, message, exchange, body and api map storage with retention expiry, and channel, exchange, event and poll progress evidence, all on plain fixtures.
- [x] Document the socket bus and page api discovery in docs 05, the sockets, streams and page api observation family in docs 03, the network observation part two kind flow map in docs 07, the honest derived observation notes in the changelog, and promote 1.1.43 with synchronized metadata, closing the network observation phase of the unlimited agentic core.

- [x] Implement the network control roadmap of 1.1.44: blockrequest, mockresponse, rewriteheaders, setcookies, readcookies, clearcookies, authflow, saveapikey, routeproxy, postform and postfiles as reviewed action kinds behind the session, plan approval and origin gates with the manifest permissions unchanged and the pinned key untouched, ten sensitive control kinds plus the read only cookie read, every rule run scoped and reverted the moment the run ends, fails or is cancelled.
- [x] Add the root netcontrol.ts module grouping the correlated rule logics (the origin naming url pattern grammar with single and double star segments, block rule registration with hit counters and idempotent reversion, mock fixture normalization and matching, header rewrite rules with set, append and remove operations and audited provenance, cookie record normalization with granted domain scoping and value redaction, proxy route normalization with the required bypass list, rate limit read parsing of remaining, limit and reset headers, retry after parsing for 429 and 503, and the rate limit wait arithmetic) and the root netauth.ts module grouping the auth and upload logics (oauth flow normalization, authorize url building with the run scoped state token, redirect code capture on the granted redirect origin with state matching, token response parsing, code exchange and refresh request building scoped to the provider token origin, revocation rules, urlencoded form payloads with UTF-8 form encoding, and multipart payloads with boundary streaming of one chunk per part), both exported through the library index with pagebridge.ts keeping the writecookies, readcookies and clearcookies seams over the page cookie jar and background.ts keeping the controlledfetch transport seam, the executors, the active rule and oauth flow maps with clean reverts and cancels at run end, and every audit as control and auth events without cookie values, key material, token values or file contents.
- [x] Gate the family three times (blockgate requiring the live session and the reviewed block rule with the explicit reviewed flag, cookiegate scoping every cookie kind to a granted domain with subdomain coverage, proxygate requiring the live session, the reviewed consent ref and the valid route with its bypass list), require the provider consent prompt ref of authflow and the explicit consent prompt ref of saveapikey, keep the rate limit wait inside the reviewed wait budget as user configured behavior with no code ceiling, require the reviewed flag on every uploaded file before any multipart upload includes it, and keep readcookies graded read only.
- [x] Add memory accessors for the block, mock and rewrite rule sets per run with hit counters and revert times, cookie operations per domain with timestamps, token metadata per provider without token values, api key entries with last use timestamps, proxy route history with apply and revert times and rate limit states that expire at their reset windows; grow the audit kinds with control events naming the origin of every applied and reverted rule; track traffic control evidence and multipart upload progress as reviewable evidence.
- [x] Document the control grammars and evidence in the protocol (the control block with applied, blocked and mocked rule counts in the outcome envelope, the auth report with token scopes but no token values or storage ids, the traffic report with rule hits and mock bodies held back, parseproposal rejecting block rules without a named origin pattern and proxy routes without a bypass list, the control kinds listed among the available capabilities) and render the traffic view in the sidepanel (block, mock and rewrite rule lists with live hit counts during the run, cookie operations, the oauth flow state with provider and scopes, token metadata with revoke buttons, the proxy state with a manual revert button, rate limit waits with reset times, api key entries with their origin scope, multipart upload progress) plus the active rule count badge in the popup and the action badge.
- [x] Extend the vitest suite to 399 tests covering rule registration and reversion at run end, mock fixtures and header operations, cookie scopes and domain refusals, rate limit waits and retry after parsing, form encodings and multipart chunk streaming, oauth code capture with state matching, token exchange and refresh, the control gates and the control options grammar of every kind, the control and auth protocol envelopes with held-back values, the parseproposal rejections, the control storage with rate limit expiry and last use timestamps, and the traffic control and upload progress evidence, all on plain fixtures.
- [x] Document the network control pipeline and the honest blocking, cookie, proxy and keystore limits in docs 03, the traffic rule, rate limit and upload streaming math in docs 05, the network control kind flow map in docs 07, the cookies, proxy and net request grant boundary in docs 01, and promote 1.1.44 with synchronized metadata, closing the network control phase of the unlimited agentic core.

- [x] Implement the debugging part one roadmap of 1.1.45: watchconsole, watcherrors and watchtasks as reviewed read only action kinds behind the session, plan approval and origin gates with the manifest permissions unchanged and the pinned key untouched, all capture scoped to the run tab by the new timelinegate with the per origin console consent prompting once and the approved decision persisting.
- [x] Add the root runtimeline.ts module grouping the correlated capture, spam, rotation and diff logics (the reviewed level set with severity ranks, the timeline source grammar, console capture with argument kinds and depth bounded serialization, secret redaction before any entry leaves the page bridge, the stack frame parser, error, rejection and long task capture with attribution names, the timeline binding to one run and its step ids, per step level floors and source filters, spam collapse with reviewed thresholds, log rotation into overflow targets without data loss, the blocking duration per step window, the failed request marking of the run, watcher detach on navigation and the console diff between two runs) exported through the library index, with extension/pagedebug.ts keeping the page-side watch (console hooks, error, rejection and resource listeners, the longtask performance observer and the clean detach), pagebridge.ts keeping the dispatch seam and background.ts keeping the executors, the active watcher map cancelled on run cancel and the killswitch, and every audit as timeline events naming the origin and scope of every watcher attach and detach.
- [x] Gate the family (timelinegate scoping capture to the run tab only, the stackgate refusing stack capture outside the granted origin, the debug wait budget gate keeping the watch window inside the reviewed wait budget, the per origin console consent before console capture begins, level floors validated against the reviewed level set and source filters against the reviewed source grammar, the required redaction pattern list before any console text is captured, spam thresholds and rotation entry counts as user configured values with no code ceiling) and allow timeline capture to run without any other action kind while console diffing grades as read only comparison evidence.
- [x] Add memory accessors for the run timeline (entries under the user configured timeline retention with per run level count summaries surviving expiry, listtimeline filtered by run, level and step id), error and rejection records with stack frames, long task entries with attribution names, rotation targets with overflow entry counts, per origin console consents and the one console diff result; track timeline capture evidence with entry, collapse, error, rejection and long task counts in progress; mark failed requests of the run as network sourced timeline entries inside the netwatch request observer.
- [x] Document the debugging grammars and evidence in the protocol (watch options per kind, the timeline block with entry counts per level and the collapse count in the outcome envelope, error and rejection ids and spam collapse counts in step details, the timeline report with entries, records and level counts, the console diff report with added, removed and repeated line classes, parseproposal rejecting watch steps for ungranted origins and level floors outside the level set, the debugging kinds listed among the available capabilities) and render the timeline view in the sidepanel (level, source and step filters; entries with spam collapse counts and step markers; errors expandable into stack frames; long task bars beside their steps; console consent prompts with approve controls; level count summary with the retention setting) plus the console diff view comparing two runs with highlighted added, removed and repeated lines, the error count of the active run and pending console consent prompts in the popup, and error level entries marked with the alert palette.
- [x] Extend the vitest suite to 426 tests covering console capture at every level with redaction and argument kinds, depth bounded serialization, stack frame parsing, error, rejection, resource failure and net failure capture, long task attribution windows and blocking duration, spam collapse thresholds, rotation without entry loss, level floors and source filters, console diff line classification, watcher detach on navigation, the timelinegate, console consent and stack gates, the wait budget gate, the debug options grammar of every kind, the timeline block and report envelopes, the parseproposal rejections, timeline storage with filters, retention expiry into level summaries, error, rejection, long task, rotation target and console consent records, and timeline progress evidence, all on plain fixtures, plus the isolated Chromium smoke test capturing the console output and one error of a fixture page through the shipped run timeline module.
- [x] Document the run timeline pipeline and the honest derivation boundary (page-injected listeners through the scripting api plus the performance buffers, no debugger permission in the manifest) in docs 03, the spam detection and log rotation math in docs 05, the debugging kind flow map in docs 07, and promote 1.1.45 with synchronized metadata, opening the debugging phase of the unlimited agentic core.

- [x] Implement the debugging part two roadmap of 1.1.46: attachcdp, detachcdp, cdpcmd, watchcdp, setbreakpoint, stepcode, watchexpr and overridescript as reviewed action kinds behind the session, plan approval and origin gates with the manifest permissions unchanged and the pinned key untouched, every kind scoped to the run tab by the new debuggate and the whole family running through the page-instrumented harness because the debugger permission stays forbidden.
- [x] Add the root cdpbus.ts module grouping the correlated devtools logics (the reviewed domain grammar and method name parsing, the domain allowlist normalization with method gates, the attach and detach lifecycle with the honest derivation note, raw command records with duration and error class, per session command serialization in send order, domain event rules with match filters and per domain counts, the breakpoint input grammar, pause state capture with call frames and dom snapshot references, the step mode grammar, watch expression values per pause, script override patterns of explicit origins and the teardown plan with the safe teardown decision that keeps the session record alive when the user detaches the debugger and pauses the run) exported through the library index, with extension/pagedebug.ts keeping the page-side instrumented harness, pagebridge.ts keeping the dispatch seam and background.ts keeping the executors, the active session map with the serialized command queue, the event rules forwarded into the run timeline, the teardown on run end, failure, cancellation, tab navigation and the user detach, and every audit as debugger events naming the origin, domains, durations and revert events without params, sources or values.
- [x] Gate the family (debuggate scoping every devtools kind to the live run tab and origin grants, the per origin debugger consent covering every requested domain before the first attach of a run with the prompt showing the domain allowlist, the grant revoking when the run ends or fails, the plan attach allowlist bounding every later raw command, the required teardown plan before approval, breakpoint conditions of the reviewed expression grammar refusing assignments and calls, the user configured breakpoint ceiling that an absent value never enforces, the reviewed watch lifetime windows kept inside the reviewed wait budget and the explicit reviewed flags of watch expressions and script override fixtures) with the honest derivation recorded on every session.
- [x] Add memory accessors for the devtools family (cdpsession records with domains and durations, raw command outcomes with durations and error classes, event rules with match counts, breakpoints with hit counts, pause states per run through listpauses with call frames and dom snapshot references expiring under the user configured pause retention while the reason and hit breakpoint survive, watch expressions with per pause values, script overrides with review provenance and debugger consents with consent and revoke times) and track devtools evidence with the family, method, domain, duration, error class, event, hit and frame counts in progress.
- [x] Document the devtools grammars and evidence in the protocol (the cdp options grammar of every kind, the cdp block with session state and command ids in the outcome envelope, pausestate summaries with call frames and dom snapshot ids, event counts per domain, the cdp report with override sources and grant prompts held back, parseproposal rejecting cdp steps for ungranted origins, attaches without teardown plans and raw commands outside the plan attach allowlist, the devtools kinds listed among the available capabilities) and render the debugger view in the sidepanel (the consent prompt with the domain allowlist shown, the session state with the honest derivation, the commands with durations and the raw protocol badge, the breakpoints with hit counts and conditions, the pause banner with call frames, the watch expression values at each pause, the script override list with revert controls, the domain event counts beside the run timeline and the pause retention and breakpoint ceiling settings) plus the debugger attached badge and pending consent prompts in the popup.
- [x] Extend the vitest suite to 450 tests covering the instrumented attach with reviewed domains and clean detach, the per session command serialization with malformed, uninstrumented and failed error classes, the domain event rules with match filters and per domain counts, breakpoint inputs and pause state capture, the step mode grammar, watch expression values per pause, script override patterns, the safe teardown when the user detaches the debugger, the page option parsing, the debuggate, the debugger consent coverage with revocation, the cdp options grammar of every kind, the breakpoint condition grammar, the user configured breakpoint ceiling, the plan allowlist resolution, the canexecute refusals, the cdp block and report envelopes with sources held back, the parseproposal rejections, devtools storage with pause retention expiry and consent revocation, and devtools progress evidence, all on plain fixtures.
- [x] Document the devtools pipeline and the honest derivation boundary (the page-instrumented harness through the scripting api, no debugger permission in the manifest) in docs 03, the devtools session, allowlist and teardown arithmetic in docs 05, the debugging part two kind flow map in docs 07, the debugger grant boundary without the debugger permission in docs 01, and promote 1.1.46 with synchronized metadata, closing the debugging part two phase of the unlimited agentic core.

## 1.1.47 debugging part three

- [x] Implement the debugging part three roadmap of 1.1.47: measureflow, heapshot, trackmemory, profilecpu, watchshifts, traceload, annotatetrace, replaytrace and capturesourcemaps as reviewed action kinds behind the session, plan approval, origin and target gates with the manifest permissions unchanged and the pinned key untouched, every kind scoped to the run tab by the new targetgate and the whole family deriving from the performance timeline buffers, the injected instrumentation probes and the existing page seams because the debugger permission stays forbidden.
- [x] Add the root profilers.ts module (21 exports: the profiler kind list, the reviewed flow metric set, the reviewed trace category list, the attach target normalizer, the flow spec normalizer, the step window resolution from the injected marks, measure with the navigation, paint, largest contentful paint, first input delay, interaction and blocking metrics, the heap interval bound of user choice only, heapsnap, growsampleof, growthtrend with the flagged steps, cpusnap with the hot function ranking, shiftentryof, tracestart, tracetofile of the derived json format, annotationof, annotatetrace aligned with the run timeline, replaytrace offline, mapurlof, capturesourcemaps, rewritesourcelocation through the minimal vlq mapping lookup and expireprofilerecords) with extension/pageprofile.ts keeping the page-side capture behind the pagebridge dispatch seam.
- [x] Gate the family (targetgate scoping every profiling kind to the live run tab with the origin grants over every non page target and the approved debugger grant of the origin covering the profiling instrumentation with the first step prompting once, the per origin source map consent before any map file is fetched with revocation removing the coverage, the reviewed flow specs of the reviewed metric set, the user chosen heap interval with no code ceiling, the reviewed growth slope, the cpu duration inside the wait budget, the user chosen shift observation window, the reviewed trace categories with the user configured byte ceiling, the required step annotations of exported traces) with the honest derivation recorded on every record.
- [x] Add memory accessors for the profiling family (flow metric series per run, heap records with byte and node counts, growth samples with computed trends, cpu profiles with hot functions, shift entries with scores and selectors, trace records with listtraces filtered by run and categories, trace file bytes, source map references and per origin source map consents with revocation) with the heavy bytes and sample payloads expiring under the user configured profile retention while the counts, hot functions, category lists and annotations survive, and track profiling evidence with the family, metric, sample, node, event, byte and flagged counts in progress.
- [x] Document the profiling grammars and evidence in the protocol (the profiling options grammar of every kind, the profile block with the metric and sample counts in the outcome envelope, flow durations per step, heap and cpu record ids, shift entries, trace records and rewritten stack locations in step details, the profile report with the source map consent prompts held back, parseproposal rejecting profiling steps for ungranted origins, unreviewed trace categories, ungranted attach targets and source map scripts, and annotation steps without reviewed annotations, the profiling kinds listed among the available capabilities) and render the profiling view in the sidepanel (the source map consent prompt with approve and revoke, the flow duration bars per step, the heap sample bars with the growth trend line and flagged steps, the hot function lists, the layout shift scores with impacted selectors, the trace records with export through the reviewed download flow and the offline replay view grouped by category and step, the attach target state of iframes and workers, and the profile retention and trace byte ceiling settings) plus the profiling badge and pending source map prompts in the popup.
- [x] Extend attachcdp with the reviewed attachtargets of page, iframe, worker and service worker (flattened sub sessions for nested target access, the service worker attached through the page controller state with derived timeline entries) and close every profiling instrument on run end, failure, cancellation and run tab navigation.
- [x] Extend the vitest suite to 475 tests covering the flow mark windows with navigation, paint, largest contentful paint, first input delay, interaction and blocking duration sums, the heap snapshot capture with the user chosen interval bound, the growth trend slopes with the flagged step thresholds, the cpu profile hot function ranking, the shift scoring with impacted selectors, the trace export with step annotations and the offline replay, the attach target grammar, the source map capture with stack rewriting, the heavy byte expiry, the page option parsing, the targetgate, the source map consent, the profiling options grammar of every kind, the settings exposure, the canexecute refusals, the profile block and report envelopes, the parseproposal rejections, profiler storage with heavy byte expiry, and profiling progress evidence, all on plain fixtures.
- [x] Document the profiling pipeline and the honest derivation boundary (the performance timeline buffers with injected marks, the page memory buffer with the dom node count, the long task attribution and event timing samples, the derived json trace format and the unobservable worker console, with no debugger permission in the manifest) in docs 03, the profiling and trace arithmetic in docs 05, the debugging part three kind flow map in docs 07, and promote 1.1.47 with synchronized metadata, closing the debugging chain.

- [x] Implement the memory part one roadmap of 1.1.49: persiststate, capturesession, restoresession, namedsessions, diffsessions, searchsessions, exportsessions and importsessions as reviewed action kinds behind the session consent gates (live session, approved plan, the restore review of every restore plan, the export review before any file leaves the device and the full record review of every import) with the manifest permissions unchanged and the pinned key untouched.
- [x] Add the root sessions.ts module (the session kind list, the session file format version, the snapshot section and search field grammars, the task state checkpoint with its corruption checksum, the snapshot, restore, auto interval, search query, folder and captured tab normalizers, the session record builder, the diff classification of tab, url, form and storage changes, the cross session search with time windows, the versioned session file export and import with checksums, the retention expiry keeping the record metadata, the filtered session list and the crash marker) with extension/pagesession.ts keeping the page seam self contained for the scripting api.
- [x] Persist the memory: task state checkpoints per run written after every completed step and resumed after service worker or browser restarts, the session event history with timestamps, saved session records with their sections, the folder tree, the stored diffs, the auto snapshot interval state with its wake through the alarms api when the browser exposes it, and the crash marker of an interrupted run detected on browser startup with the crash restore prompt inside the consent model.
- [x] Extend the vitest suite to 527 tests covering the task state checksums and corruption refusal, the crash marking, the snapshot, restore, interval, query, folder and tab grammars, the diff classification, the search matching and time windows, the export and import round trips with checksums, the retention expiry with no code ceiling, the filtered listing, the session consent gates with the restore review and origin skips, the name and folder uniqueness, the options grammar of every kind, the parseproposal rejections of restores reopening ungranted origins and import files of unknown format versions, the session block and report envelopes, the session progress evidence and the session memory store, all on plain fixtures.
- [x] Document the session persistence model in docs 03, the session memory arithmetic in docs 05, the session kind flow map in docs 07, and promote 1.1.49 with synchronized metadata, opening the memory phase and closing the perception, media and network phase of the roadmap.

## 1.1.50 workflow engine part one

- [x] Implement the workflow engine part one roadmap of 1.1.50: composeworkflow, savetemplate, runworkflow, dryrun, delay, waitelement, compute and extractvars as reviewed action kinds behind the session consent gates (live session, approved plan, origin grants inside the session and the explicit run review flag of every real run) with the manifest permissions unchanged and the pinned key untouched.
- [x] Add the root workflow.ts module (the workflow kind list, the step, block, invocation, template, binding, expression, regex rule, delay and wait normalizers, composeworkflow with block expansion and freezing, validateworkflow over kinds, scopes and bindings, the typed scope stack with pushscope, popscope and outward resolution with shadowing, bindvariables with coercion refusals, expressioneval with arithmetic, comparison, logic, text, contains and length operators, regexextract with honest no match outcomes, the seeded delayjitter, waitelementplan, the run lifecycle of newworkflowrun, pauserun, cancelrun, runstep, runworkflow with per step checkpoints and the gates, and the pure dryrunworkflow with read only projections) with the engine staying pure through the injected executor and risk callback.
- [x] Execute the family in the background: every workflow step dispatches through the same consent gate and dispatch chain as a plan step (a step becomes a tool step of its own kind so no construct bypasses review), the delay executor rides the alarms api beyond the service worker lifetime, the element wait executor polls through the page bridge and aborts cleanly on tab navigation, compute and extractvars record provenance, the dry run refuses steps without a read only projection, single step execution runs one chosen step outside the run loop, pause, resume and cancel reach a run mid flight, and the startup hook marks interrupted runs paused at their last checkpoint so the resume continues exactly there.
- [x] Persist the memory: workflow record versions with timestamps, every workflowrun with its state transitions, the runlog of every execution under the user configured retention with no code ceiling, the variable values per scope for inspection after runs, the expression results and regex captures as provenance and the shareable step templates under unique names; track workflow completion as executed steps over total steps in progress.
- [x] Document the workflow grammar, the variable scopes and expressions and the dry run contract with single step debugging in docs/workflowautomation.md, the workflow engine model in docs 03, the engine arithmetic in docs 05, the workflow kind flow map in docs 07, and promote 1.1.50 with synchronized metadata, opening the workflow phase of the roadmap.
- [x] Extend the vitest suite to 559 tests covering the composeworkflow grammar rules, the three level block expansion with cyclic and unknown refusals, validateworkflow over kinds, scopes, bindings and inputs, scope resolution and shadowing, binding coercion failures, the expression operators with coercion and mismatch refusals, regex named captures with optional groups and no match cases, the seeded delay jitter bounds across sampled runs, the element wait poll plan, the run lifecycle, single step execution and interpolation refusals, the run loop with checkpoints, block scopes and the session, plan and origin refusal paths, failure with checkpoint resume, dry run purity with projection refusals, the workflowgate with the run review flag, the workflowproposal and workflowoutcome envelopes with the parseproposal rejections, workflow storage with runlog retention and provenance, and workflow progress evidence, all on plain fixtures.

## 1.1.51 workflow engine part two

- [x] Implement the workflow engine part two roadmap of 1.1.51: condition, branch, loop, repeatuntil, whileloop, foreach, parallel and trycatch as reviewed action kinds behind the session consent gates (live session, approved plan, origin grants inside the session and the explicit run review flag of every real run) with every child step dispatched through the same consent gate and dispatch chain as a plan step so no control construct bypasses review, the manifest permissions unchanged and the pinned key untouched.
- [x] Add the root controlflow.ts module (the control flow kind list, the payload normalizers of every construct with the grammar rules of unique branch paths with a mandatory else, distinct item and index variables, optional loop bounds with a documented default and a mandatory while bound, unique parallel branch ids with reviewed join policies, try blocks with catch handlers, retry policies and timeout policies, the cancelled error class, condition evaluation over extracted values, branch selection by page state through the snapshot accessor, the loop runners with deep copied items and rebound bindings per iteration and nested index paths, the foreach element resolution, the parallel runner with isolated branch scopes, the join with first, last and fail strategies and cancel or continue on failure, the try runner with catch, rerun, retry and timeout, the seeded backoff windows, the step and run timeout races, the control summaries, and the executor wrapper) with the engine staying pure through the injected executor, resolver and page state seams.
- [x] Execute the family in the background: the run executor routes control kinds to the control engine with the page state accessor and the element resolver of the page bridge queryelements seam, the merged scopes and the iteration runlog of a control step flow back into the run loop so checkpoints, resume and the audit trail see every child outcome, the run level timeout budget cancels the run with the cancelled error class, control decisions persist per run and audit as branch, loop, join, catch, retry and timeout events, loop iterations count toward the user defined denominator in progress, the single step and resume paths route control kinds through the same engine, and the setloopbound handler recomposes a loop bound as a new record version while the older version survives.
- [x] Document the control flow grammar in the protocol (the control options of every kind, the join strategies and merge conflicts, the control summaries of the workflow proposal, the control decisions of the workflow outcome, the timeout and retry blocks of the response envelope reported distinctly, the control decisions of the workflow report, and the control kinds listed among the available capabilities) and render the family in the sidepanel (the chosen branch path highlighted, loop iterations as collapsible groups, retry attempts with backoff countdowns, parallel branches as concurrent timeline lanes, the join result with merged variables, try blocks with the catch path, timeout aborts with the exceeded budget, the loop safety bound editor before a run and the else path preview during review) plus the active loop iteration count of running workflows in the popup.
- [x] Extend the vitest suite to 600 tests covering the control kind risk grades, the payload normalizers with their refusals, the nested child collection, the review summaries, condition evaluation, branch selection by match and page state, loop rebinding with bound overflow and nested paths, repeat until convergence and overflow, while loops at and under the bound, foreach with resolved elements and the empty match, parallel execution with scope isolation and join merging, the join conflict strategies, cancel and continue on branch failure, try catch with error classes, rerun and handler failure, retry backoff shapes with seeded jitter and exhaustion, per step and per run timeout aborts with the cancelled error class, the run loop adoption of control steps behind the gates, the control grammar validation with hidden child refusal, composition grading by the worst child, dry run projections, the control summaries of proposals, the branch and loop outcome envelopes with distinct timeout and retry response blocks, control flow decision storage with the branch history, and loop progress over the denominator, all on plain fixtures.
- [x] Document the conditionals and branching, the loops with safety bounds and foreach, and the parallel branches with joins and merge strategies in docs/workflowautomation.md, the control flow engine model in docs 03, the control flow arithmetic in docs 05, the control kind flow map in docs 07, and promote 1.1.51 with synchronized metadata, closing the workflow engine part two step of the roadmap.

## 1.1.52 triggers and scheduling

- [x] Implement the triggers and scheduling roadmap of 1.1.52: visitrule, urlrule, menurule, keyrule, buttonrule, cronrule, intervalrule, urllistrule, webhookrule and eventrule as reviewed action kinds behind the session consent gates (live session, approved plan, origin grants inside the session and the explicit arm review of every rule before it arms) with every launch re-passing the same run gates through the runworkflow machinery, the manifest permissions unchanged and the pinned key untouched.
- [x] Add the root trigger.ts module (the trigger kind and family lists, the family payload normalizers of every rule grammar, the five field cron parser with named weekdays and months and the timezone aware next fire computation through the runtime timezone database, the interval scheduler with the seeded jitter spread, the glob url matcher with ports, the visit origin matcher, the cooldown suppressor, the dedupe keeping one pending fire per rule while a run is active, the trigger queue with its arrival order drain, the url list planner, the webhook verifier of the shared secret and payload schema, the observeevents subscription seam, the pause and resume suspension with the resume drain, the trigger evaluation skipping disabled, paused, unreviewed or cooled down rules, the manual run step preview with its confirmation outcome and the rule summaries of the review panel) with the engine staying pure through the injected launch seam.
- [x] Execute the family in the background: the arm executor validates the composed workflow and its grants over every match origin, the tabs onUpdated listener evaluates visit and url rules and the navigate event, the page observation seam (watchmutate, watchfocus, watchbanner, watchconsole, watcherrors) evaluates event rules through observeevents, context menu entries register from menu rules when the context menus api is exposed, command listeners route key rules, the popup open evaluates the button rule behind the gates once, cron and interval schedules walk forward on every service worker wake with the persisted next fire times and the opportunistic alarms ride when the browser exposes them without a declared permission, the session pause suspends every rule and the resume drains the queue through the same gates, manual runs render the step preview before confirmation, and the trigger fired notification rides the notifications api when exposed with the badge always announcing.
- [x] Persist the memory: trigger rules with their workflow references and runtime states, fire records under the user configured trigger retention with no code ceiling while the rule counters survive, the last and next fire times per rule, rulestats counters for the history view, verified webhook payloads only after secret verification, the trigger queue and manual run previews with their confirmation outcomes; audit rule enable, disable, arm, fire and cooldown suppression events; track trigger evidence with the fire, launch and suppression counts in progress.
- [x] Document the trigger grammar and evidence in the protocol (the trigger options grammar of every kind, the triggerlist envelope with workflow names and queue depth, the triggerfired notification envelope, the manual run preview envelope, the next scheduled fire time per rule in the response envelope, parseproposal rejecting trigger steps without the arm review or with origins outside the grants, and the trigger kinds listed among the available capabilities), render the trigger list view in the sidepanel (enable and disable toggles, the next scheduled fire of cron and interval rules, the fire history, the visit rule creator from the current page, rule duplication to a second workflow, the manual run step preview with approve and cancel, the webhook status with secret rotation and the trigger retention setting) plus the armed rule count, queued fires held while the session is paused and the button rule status in the popup.
- [x] Extend the vitest suite to 632 tests covering the trigger kind risk grades, the family payload normalizers with their refusals, the glob matching across segments and ports, the visit firing on navigation, the cron parsing with named fields, or semantics, sparse schedules and timezones, the interval jitter spread, the due listing, the cooldown suppression with the remaining window, the disabled, paused and unreviewed refusals with dedupe, the queue drain, the pause and resume paths, the url list planning, the webhook secret verification with payload rejection, the event subscription through observeevents, the manual run step preview with the confirmation outcome, the trigger grammar validation, the trigger gate, the rule match origins, the triggerlist and triggerfired envelopes with the next fire time, the parseproposal rejections, trigger storage with fire retention, queue, webhook payloads and manual run previews, and trigger progress evidence, all on plain fixtures; document every trigger kind, the cron and interval grammar, the webhook setup with secret rotation, the manual runs with step preview, the keyboard and context menu bindings and the cooldown, dedupe and queue discipline in docs/triggerguide.md, the trigger engine model in docs 03, the trigger arithmetic in docs 05, the trigger kind flow map in docs 07, cross link the trigger rules from docs/workflowautomation.md, and promote 1.1.52 with synchronized metadata, opening the triggers and scheduling step of the workflow phase.

## 1.1.53 workflow editor interface

- [x] Implement the workflow editor interface roadmap of 1.1.53 as a surface release with no new action kinds: the reviewed vocabulary stays 333 kinds, the manifest permissions stay unchanged and the pinned key stays untouched while the whole engine and trigger layer becomes editable end to end behind the same consent gates.
- [x] Add the root workfloweditor.ts module (the canvas model of nodes, typed binding edges, block definitions, layout state, the mini map and the undo and redo stacks; loadworkflow collapsing each contiguous block region into one invocation node with a unique id and lifting every binding into a typed edge; saveworkflow routing snapped steps into their block definitions with duplicate id, unknown block, backwards edge and unknown endpoint refusals and the full grammar recomposition; the grid snapping with block column attachment, the reorder, the grouping into blocks, the palette and the step library of every reviewed kind grouped into the five categories with option schemas, the template insertion with nested params, the mini map projection with viewport math and click navigation, the zoom with label scaling, the step search, the breakpoint markers on any step including block internals, the debug run segmentation, the version diffing, the json and yaml file format with the documented yaml subset, the per site override application and the undo and redo stacks covering every canvas edit).
- [x] Execute the editor in the background: the editorsave handler behind the editorsavegate of a live session, an approved plan, unique node ids, forward only edges and the full grammar storing the record, version, layout and breakpoints; the editormodel reopen; the import handler grading records unreviewed behind the import review with approve and reject; the export and share handlers behind the export content review refusing secret fields; the rollback grading unreviewed like an import; the diff and run history handlers with filters and retention; the background run toggle with checkpoint restore on every worker wake through the same gates; the watchdog scan with retry, pause, cancel and zombie reaping and persisted events; the per site override handlers; the debug run that pauses exactly before the next breakpoint; and the startup hook marking interrupted runs paused with the interrupt pause kind.
- [x] Render the editor tab in the sidepanel: the draggable canvas with typed sockets and block containers with nested child steps, the searchable palette and step library, the step inspector with options, bindings and nested params, the variable inspector, the run log with breakpoint marks, the run history with filters, the version timeline with diffs and rollbacks, the import review before activation, the export and share buttons, the background run toggle, the watchdog status and the per site override editor.
- [x] Persist the memory: workflowversion records with change notes and rollback markers, versiondiff results for the history view, runhistoryentry records under the user configured retention with the gethistory filters, editor layouts so the canvas reopens as left, breakpoints per workflow, siteoverride records, watchdog events with recovery outcomes, pending workflow imports and the per workflow background run flags.
- [x] Extend the vitest suite to 672 tests covering the palette and library, the load and save round trips with repeated invocations and binding edges, the save refusals, the canvas edits with undo and redo, the nested param binding, the template insertion, the search, the mini map and zoom math, the version diff, the json and yaml round trips with the import rejections and the yaml subset, the breakpoint planning and the debug pause and resume, the editor policy gates, the per site override application with refusals, the engine additions of nested param binding through block scopes, the background checkpoint restore, the watchdog stall detection, recovery and zombie reaping, the editorstate and runhistory envelopes, and the editor memory, all on plain fixtures; document the canvas editor and palette, the versioning, diffing and templates, the background runs and the watchdog, the per site overrides and the import, export and sharing in docs/workflowautomation.md, the editor model in docs 03, the editor arithmetic in docs 05, the editor surface flow map in docs 07, and promote 1.1.53 with synchronized metadata, closing the workflow editor step of the workflow phase.

## 1.1.54 agent protocol part one

- [x] Implement the agent protocol part one roadmap of 1.1.54: the browser becomes a tool server over mcp with json schema tool listings, capability negotiation, json rpc framing, a stdio bridge for local clients and the localhost bind as the documented default, adding the single read kind `listruns` (333 → 334 kinds) for the memory run listing, with the manifest permissions unchanged and the pinned key untouched.
- [x] Add the root `mcpserver.ts` module (the json rpc frame grammar with newline delimited and http post envelopes and the parse and serialize round trips, the frame validation of malformed ids, unknown methods, bad params and the user configured frame size, the rpc error codes mapped onto the classic numbers with consentrefused reserved at -32001, the methodentry routing table of initialize, ping, tools/list, negotiate and tools/call, the initialize handshake with the server info, the ping keepalive, the tools/list report with every tool's version, schema, risk grade and consent metadata, the capability negotiation over the protocol version, tool floor and transports, the client records with pairing state, the per client request queue serialization under the user configured depth with an absent depth unbounded, the consent gated tool dispatch, the localhost bind, the stdio bridge launch, relay and restart with the native messaging host manifest, the framed logs that never carry payloads and the tool call records for the audit trail) and the root `toolcatalog.ts` module (the four namespaces of browser, workflow, memory and system with namespaced and versioned tooldefs, the json schema generation with typed properties, required markers and default values, the plain language descriptions stating the consent class and side effects, the consentmeta of every tool with side effects and the collision free name resolution).
- [x] Execute the protocol in the background: the mcpserverconfig handler behind the serverbindgate and serverenablementgate with every knob a user choice with no code ceiling, the start and stop handlers (the server never starts without the explicit enablement and a non localhost bind never passes without the explicit remote review), the pairing decision and disconnect handlers, the bridge launch and restart through the native messaging host when the browser exposes it with the honest refusal otherwise, the frame intake that gates on the running server and the allowed transport, registers one client record per transport and serializes concurrent frames per client in arrival order, the routed frame path that audits initialize, tools/list, negotiate and every tools/call with the client, tool and origin but never the payload, the negotiated capability set stored on the client record, tool call records under the user configured call retention, the toolcall evidence in the plan progress and the tool block of the response envelope — every dispatched step runs through the same executor chain as a reviewed plan step so no tool call bypasses review.
- [x] Extend policy with the protocol gates (validatetoolcatalog against the action kind grammar with namespaced unique names, domain membership, typed properties with their required list and non empty property maps; toolriskgrade re-deriving every tool's risk from its kind; toolconsentrequired demanding consentmeta on side effects; toolversionfloor; toolnamespacegate; serverbindgate grading a non localhost bind sensitive behind the explicit remote review; serverenablementgate demanding the explicit enablement before anything listens) and the protocol with the toolcall and toolresult frames, the tool list and schema grammar, the capability negotiation fields, the stdio frame delimiting and native host contract, the namespaces and the tool error codes of the response envelope.
- [x] Persist the memory: client records with pairing state and the negotiated capability set per client, stdiobridge launch events with process ids and restart markers, the mcp server config and runtime state, and tool call records with the client, tool, origin, outcome and refusal code under the user configured call retention; the sidepanel renders the agent protocol tab (server status with start and stop, the bind state and port, the connected clients with their transports and capabilities and the pairing prompt, the disconnect control, the tool catalog grouped by namespace, the bridge status with restart and the recent tool calls with caller and outcome) and the popup shows the mcp running indicator.
- [x] Extend the vitest suite to 710 tests covering the frame round trips of both wire formats, the malformed wire rejections with the rpc error codes, the frame validation against the routing table and the user configured frame size, the payload free framed logs, the initialize handshake and ping, the tools/list schema emission with risk grades and consent metadata, the capability negotiation happy path and mismatches, the client records with pairing decisions, the request queue serialization per client under the configured depth, the localhost binding defaults, the read only dispatch under the dryrun risk class, the sensitive dispatch only as the named approved plan step, the unpaired, disconnected, dead session, unapproved plan and out of grant refusals, the unknown tool, ambiguous bare name and version floor refusals, the executor failure as internal error, the method table routing, the consent error code mapping, the stdio bridge relay and restart, the toolcall and toolresult frames, the catalog namespaces, schemas, descriptions and collision resolution, the policy tool schema validation, risk grading, consent requirement, bind and enablement gates and the dispatch gate, the protocol tool frames and envelope, and the memory client, bridge, audit and tool call persistence, all on plain fixtures; document the mcp server and tool list, the json rpc framing and error codes, the stdio bridge setup and the capability negotiation with localhost binding in docs/agentprotocol.md, the agent protocol model in docs 03, the agent protocol arithmetic in docs 05, the agent protocol surface flow map in docs 07, cross link the protocol from docs/workflowautomation.md, and promote 1.1.54 with synchronized metadata, opening the agent protocol phase of the roadmap.

## 1.1.55 agent protocol part two

- [x] Implement the agent protocol part two roadmap of 1.1.55 as the remote surface of the tool server: the stream http transport with tls, session tokens for pairing, tool namespacing scopes, tool version floors per client, client allowlists and the auth handshake make the remote surface safe — nothing runs without a paired, allowed and authenticated client — with no new action kinds (the reviewed vocabulary stays 334 kinds), the manifest permissions unchanged and the pinned key untouched.
- [x] Add the root `clientauth.ts` module (the one time pairing codes with their single use state, the session tokens issued in the exchange that persist only as sha-256 tokenhash values through the platform crypto seam, the token verification on every frame with scopes, expiry and revocation, the allowlist grant and check with the grant history, the auth challenges with single use nonces, the handshake completion and the fixed auth refusal message that leaks no pairing state) and the root `httpstream.ts` module (the http stream config with the posted endpoint and event channel paths, the stream channels with heartbeats and dead channel close, the tls termination that requires valid certificates before remote traffic, the user configured client ceiling with an absent value unbounded, the remote status report and the ordered frame pipeline where tls terminates first, the token verifies, the allowlist checks and the namespace scope runs before any consent gate).
- [x] Add the root `approvalgate.ts` module: the approval gates of sensitive remote tool calls with the full call arguments, the secret redaction of the user marked fields, the prompts carrying the client identity and called tool, the approve and refuse resolution with the actor and latency record and the configurable timeout that refuses an unanswered gate by default.
- [x] Extend policy with the remote gates (consentmetagrade deriving the risk class, approval requirement and session origin scope of every sensitive tool into the consent metadata and the tools/list report; allowlistentryvalid against the known identities; tokenlifetimevalid and approvaltimeoutvalid as user configured positive values; remotetransporttls demanding tls for any non localhost transport; pairingreadinessgate refusing pairing without a live session; remoteenablementgate grading the remote enablement sensitive behind the explicit remote review; tokenscopevalid limiting scopes to the granted namespaces; revocationgate keeping the revocation always available) and the protocol with the httpstream, pairing, token, approval, tls, allowlist and heartbeat envelopes.
- [x] Execute the remote surface in the background: the pairing issue behind the pairing readiness gate, the challenge and exchange intake that redeems one code once and issues one token while the raw value leaves exactly once, the allowlist and remote config handlers behind the remote enablement gate, the always available revocation, the approval decisions that execute or refuse exactly the held calls with the actor and latency audited, the ordered http frame intake with the client ceiling, token verification, allowlist and scope checks while sensitive calls of remote clients land behind the approval gates, the maintenance sweeps that expire tokens and unanswered gates with audits, the channel heartbeats and the audits of pair, revoke, expire, allow and refuse events.
- [x] Render the remote surface in the sidepanel (the remote transport status with the tls state and channel health, the pairing flow with the copyable one time code, the paired clients with scopes, token expiry countdowns, per client tool floors and one click revocation, the approval gate cards with the redacted arguments and approve or refuse, the allowlist editor with namespace scopes, the auth failures with their json rpc error codes and the tls certificate options with the endpoint, client ceiling, token lifetime and approval window as user choices) and surface the pending approval gates as notifications in the popup.
- [x] Extend the vitest suite to 750 tests covering the stream channel and heartbeat math with the idle window, the tls requirement enforcement, the max client refusal, the pairing issue, exchange and single use, the token verification with scopes and expiry, the allowlist refusal paths, the auth challenge handshake completion, the approval gate raise, approve, refuse and timeout refusal, the namespace scoping and the ordered pipeline, the per client tool floor negotiation, the secret redaction, the remote status reporting, the new policy gates, the part two protocol envelopes and the token, pairing code, allowlist, approval, handshake and identity persistence, all on plain fixtures; document the http stream transport, the pairing, tokens and scopes and the approval gates, tls and the auth handshake in docs/agentprotocol.md, the remote model in docs 03, the remote arithmetic in docs 05, the remote surface flow map in docs 07, and promote 1.1.55 with synchronized metadata.

## 1.1.57 llm integration

- [x] Implement the llm integration roadmap of 1.1.57 as the heart of the product vision: any llm drives the extension through user configured gateways, base urls, api keys and models — nothing is hardcoded, no provider allowlist exists and the four protocol styles are wire shapes the user picks — with no new action kinds (the reviewed vocabulary stays 334 kinds), the manifest permissions unchanged and the pinned key untouched.
- [x] Add the root `llm.ts` module (the per style request shaping with the key placement of the bearer header, the x-api-key header and the url query, the per style response parsing with usage counts, the completion call through the fetch machinery with reviewed retries and backoff, the local model calls that refuse non loopback endpoints, the per style streamed token parse, the command parsing with the deterministic intent classifier, the plan drafting, the replan, the reflection with running lessons, the openapi style tool briefs, the parse guardrails of code fence stripping, schema validation, retries and the refusal after exhaustion, and the usage records with the cost budget halts), the root `modelroute.ts` module (the task kind resolution, the availability marking and the user configured fallback) and the root `promptlibrary.ts` module (the variable extraction, the render with the consent notice requirement of sensitive flows, the versioning with change notes and the search of user templates only).
- [x] Extend policy with the model gates (providervalid of the user configured endpoint, model list, wire shape and storage id auth reference; provideregressgrade of every call as a data egress event; egressconsentgate that refuses ungranted page content; localsensitivegrade preferring local endpoints; plandraftreviewgate requiring the draft review; replanreviewgate requiring the fresh review of revised tail steps; costbudgetvalid of positive user ceilings; guardverdictgate refusing invalid or refused output; planlint of drafted steps against the action grammar) and the protocol with the modelproposal and modeloutcome envelopes.
- [x] Persist the model state in memory: provider configs with references and never secrets, the routing table with its revision history, every usage record with run and step ids and the period totals of getusage, the plan drafts and replans, the reflection notes, the prompt template versions with change notes, the cost budgets, the latest parsed command and the guard refusal notices; the api keys stay in the browser credential store behind their storage ids.
- [x] Execute the llm integration in the background: the provider save, test call and removal handlers, the local endpoint save and health check, the route editor with revision bumps, the command parse with the deterministic and local fallbacks, the plan drafting with lint, the draft decision that turns an approved draft into a pending plan behind the same plan review, the replan and fresh review decisions, the reflection of executed steps, the budget setter, the template library handlers and the llm state snapshot, with every provider call landing its usage record and its data egress audit entry with endpoint, model and token counts and every guard refusal landing its notice.
- [x] Render the models tab in the sidepanel (the provider list with endpoint editing and test call buttons, the modelroute table per task kind, the local model endpoint with its health check, the model drafted plan review cards with editable steps and approve or reject, the replan reviews with the highlighted changed tail, the reflection notes under the completed steps, the budget meter against the configured ceiling, the prompt library browser and the guard refusal notices) plus the natural language command box at the panel top with the intent badge, and the popup shows the active model per task kind.
- [x] Extend the vitest suite to 861 tests covering the request shaping and response parsing of every protocol style, the provider calls against a stubbed endpoint with the refusal paths, the local model calls with no external network, the streamed tokens, the command parsing across the intent kinds and the confidence thresholds, the plan drafting with grammar lint, the replan keeping completed steps, the reflection with running lessons, the guardrails with retries and refusal after exhaustion, the tool briefs, the usage accumulation and budget halts, the modelroute resolution and fallback, the prompt library render and versioning, the new policy gates, the envelopes and the persistence, all on plain fixtures; document the provider configuration and routing, the command parsing and intents, the plan drafting, review and replanning, the reflection and budgets and the prompt library and guardrails in docs/llmguide.md, the llm integration model in docs 03, the llm arithmetic in docs 05, the llm surface flow map in docs 07, and promote 1.1.57 with synchronized metadata.

## 1.1.58 multi agent part one

- [x] Implement the multi agent part one roadmap of 1.1.58 as the opening of the multi agent phase — several agents working at once over one shared context, each in its own tab with its own role — with no new action kinds (the reviewed vocabulary stays 334 kinds), the manifest permissions unchanged, the pinned key untouched and every agent proposal passing the same human review, because multi agent coordination never bypasses review.
- [x] Add the root `multiagent.ts` module (agent registration bound to one tab under the one agent per tab rule, role assignment with the defaults of planner, worker and observer plus custom user roles, sub agent spawning under a spawnrequest that refuses recursion past the user configured depthlimit, pauseone, resumeone, stopone, the killall killswitch that halts every agent at once, the per agent budget check, the per agent scope gate, the per agent usage accumulation, the per agent run contexts reusing the workflow engine, the lifecycle events and the swarm overview), the root `taskqueue.ts` module (enqueue into user configured lanes with user configured priorities, claim of the highest priority task with the oldest tie winner, work stealing that respects the lane ownership rules, completion with claim release, claim heartbeats, the requeue of orphaned tasks past the user configured window, cancellation, the lane report and the completion policies) and the root `blackboard.ts` module (posting with authors into the goals, facts, findings and scratch sections, reading with freshness filters, retirement by the user configured window, cross agent visibility and the consent class inheritance of the source extraction), plus the root `agentmailbox.ts` module (direct, broadcast and role addressed routing, inbox and outbox records with unread counters and the receive drain with ack tracking).
- [x] Extend policy with the swarm gates (queuelanesvalid, workstealgrade of one user approved swarm with lane ownership, agentbudgetvalid, agentscopevalid against the session grants, spawngrade with the role risk class, killswitchgate with no configuration barrier, messageegressgrade of page content carrying messages and blackboardconsentgrade), the protocol with the swarmstate envelope and the agents/notify event frames plus the documented queue, mailbox and blackboard grammar, and memory with the agent, queue, mailbox, blackboard, killswitch, spawn history, agent event and per agent usage records and the swarmoverview accessor.
- [x] Execute the swarm in the background behind the same consent gates: the swarmagent handler (register, assign, bind, spawn, pause, resume, stop, killall with claims release, disarm, budgets, scopes, heartbeats), the swarmqueue handler (configuration, enqueue, claim behind the killswitch, state and budget gates with the per agent run context, steal behind the work steal grade, complete with the usage counters, cancel, requeue), the swarmmailbox handler (send with the egress grade, receive) and the swarmblackboard handler (post, retire, sweep), with every register, assign, claim, steal, complete, spawn, killswitch and pause event landing its swarm audit entry.
- [x] Render the agents tab in the sidepanel (agent cards with role, tab, state, current task, budget use and heartbeat with pause, resume, stop, claim and heartbeat buttons, the killswitch button with confirmation, the spawn dialog with role, task and depth, the task queue view with lanes, priorities and claims plus enqueue, requeue and cancel, the mailbox view with the compose form for user messages, the blackboard view with its editor and retire buttons, the spawn history and the lifecycle event feed), and the popup shows the swarm size and the active task count.
- [x] Extend the vitest suite to 921 tests covering agent registration and role assignment, the one agent per tab binding, enqueue, claim and complete with priority ordering, work stealing with lane ownership, heartbeat expiry and requeue, the mailbox send, receive, broadcast and role addressed routing, the blackboard post, read and retire with freshness and cross agent visibility, the spawn depth limit refusal, pauseone isolation, killall cancellation, the per agent budget halts, the agentscope refusal paths, the queue, budget and scope validation gates, the swarmstate and agentevent envelopes and the queue, mailbox and blackboard persistence, all on plain fixtures; document the swarm in docs/multiagent.md, the multi agent model in docs 03, the multi agent arithmetic in docs 05 and the multi agent surface flow map in docs 07, and promote 1.1.58 with synchronized metadata.

### 1.1.60 execution environments

- [x] Add the execution environment contracts to types.ts (environmentkind with pagecontext, isolatedworld, offscreenworker and sandboxframe, the environmentrequirement profile, workerrequest and workerresponse envelopes, sandboxrender and sandboxrenderresult with their per render nonce, keepalivestate with start, heartbeat, stop and reattach events, runstaterecord with the pending step, url history, environments, turnarounds and provenance, workerevent, offscreenregistryentry, runlock and sealedrunstate) plus the toolstep environment field, the session environmentgrants joining the origin grants, the observation and stepoutcome environment fields, the parseoffload, workerpoolsize, runstateretention, sandboxorigins, keepaliveinterval, zombieintervals and runstatebytes run settings and the environment, worker and sandbox audit kinds.
- [x] Implement the environments module (the per kind requirement table where evaluate is isolatedworld only and untrusted markup is sandboxframe only, the offload families, the executor registry, the routing with the inline fallback, the worker traffic envelopes with transferables and partial streaming, the pool plan with no engine cap, the offscreen document lifecycle and the isolated world injection with reviewed arguments only), the sandboxframe module (script and handler stripping, the per render nonce, the postmessage envelope, the nonce checked acceptance and the render provenance) and the runstate module (the keepalive lifecycle, the url history, the environment and turnaround records, the recovery plan, the zombie sweep, the run lock with expiry, the shared tab serialization, the integrity sealing, the quota pruning and the export).
- [x] Add the eight environment gates to policy.ts (stepenvironmentvalid, environmentgrantgate, offscreencapabilitygate, keepalivegate, keepaliveintervalvalid, workerpoolsizevalid, sandboxorigingate and environmentrequirements), the environment grammar and environment report envelopes plus the environment beside the step outcome in protocol.ts, and the environment and turnaround progress evidence.
- [x] Extend the manifest with the optional offscreen permission, the offscreen declaration with its reasons and justification, the sandbox page with no extension privileges and the isolated world content script registration with an empty match list; allow the sandbox key and the offscreen declaration in the cli manifest validator while keeping the privileged pages out of the sandbox.
- [x] Execute the environments in the background behind the same consent gates: the capability negotiation through the permissions api, the environment routing before every dispatch with the sandbox, isolated and offscreen branches, the keepalive port with its heartbeat cadence, the persisted run state with restart recovery, the zombie reaping, the run lock, the shared tab serialization, the offscreen document lifecycle and the environment, worker and sandbox audits.
- [x] Render the environments in the sidepanel (the environment badge on each step row, the environments panel with the keepalive trail, the recovery notice, the zombie warning, the grouped steps with turnarounds, the grant editor, the capability request, the registry, the offscreen documents, the worker feed, the url history, the locks and the per profile options) and the popup (the environment status line with the heartbeat indicator, the zombie reap and the offscreen grant request).
- [x] Extend the vitest suite to 1012 tests covering the routing of every reviewed action kind, the isolatedworld and sandboxframe exclusivity, the offload families, the executor registry, the routing fallbacks, the worker round trips with transferables and partial streams, the pool plan, the offscreen lifecycle, the isolated injection, the script and handler stripping with noncing, the keepalive heartbeat emission and port closure, the reattach, the url history, environment and turnaround records, the recovery plan, the zombie detection and reaping, the concurrency lock with expiry, the shared tab serialization, the integrity sealing with tamper refusal, the quota pruning, the export, the eight gates, the two envelopes, the sealed per profile persistence with expiry, quota and provenance, the environment and turnaround evidence and the new manifest keys, all on plain fixtures; document the environments in docs/14.executionenvironments.md, the environment model in docs 03, the environment arithmetic in docs 05 and the environment surface flow map in docs 07, and promote 1.1.60 with synchronized metadata.

### 1.1.61 security part one

- [x] Add the security part one contracts to types.ts (sensitiveclass with payment, credential, delete and publish, logeventkind, loghash with previous, current and algorithm, immutablelogentry with origin and step provenance, sealedlog with the final hash, storedrunlog, originprofile with per kind grants and denials, automationallowlistentry scoped per profile workspace, consentwindow with start, duration, scope and boundary fields, consentscope naming origin, kinds and boundary in one grant, revokerunevent with the halted step ids, classconsent per origin, maskrule for field shapes per origin, deniedevidence, revocationevidence and the securityview) plus the grant, expiry, revoke, deny, seal and mask audit kinds and the consentduration, logretention and maskshapes run settings.
- [x] Implement the originpolicy module (the denydefault allowlist checks with exact origin matching and no wildcard expansion, the active tab as exactly one explicit single origin grant, the origin profile grammar with per kind grants and denials, the sensitive classification into the four classes refined by kind options with uploads, downloads and evaluate sensitive by default and credential bearing submits in the credential class, the fresh class consent math per origin, the consent windows with named boundaries and explicit prompt renewals, the terminal revokerun halting the pending and queued steps, the consentscope grants, the consent prompt text and the popup summaries), the immutablelog module (the sha-256 chain derivation written at append time, the append only store with no update or delete path, the terminal completion seal, the read time verification with tamper evidence, the verified chain reports and exports) and the maskinputs module (the documented password, token, card and secret shape families with user extensions, the typed value, form state, observation, stored value and export masking that keeps field shapes while values carry the redaction marker).
- [x] Add the eight security gates to policy.ts (automationallowlistgate with the denydefault posture, originprofilegate before sensitive kinds, consentwindowgate with the one session one origin scope and the mid step suspension, revokerungate as terminal, sensitiveclassgate routing every class through its fresh prompt per origin, sensitivepipelingate, consentdurationvalid with no unlimited default and logreadgate refusing broken chains), the consentmodel, securityreport and logchainreport envelopes in protocol.ts, and the denied step and revoked run evidence in progress.ts.
- [x] Persist the security records in memory.ts through the adapter seam documented for the future append only backend: the automation allowlist per profile workspace with add and remove, the originprofiles, the consentwindows with the expiry sweep of closed windows, the revocation history visible for later consent prompts, the fresh class consents per origin, the mask rules per origin with add and remove, the immutable run logs with the verified chain export that refuses broken links and the sealed log retention that keeps the seal hash while the entries reduce to their chain summaries.
- [x] Execute the security gates in the background behind the same consent chain: the allowlist, profile, window, class consent and revocation checks before every dispatch with denied events recorded without navigation, the masked step outcomes and the appended step transitions in the session immutable log, the consentscope grant written at session start with the active tab allowlist entry, the log sealed at session close, the consent window expiry suspension with the resume refusal, and the security handler (allowlist add and remove, profile kind grants and denials, consent window open, renew and class consents, the mid run revoke with the halted step rollback, the mask rules, the verified log reads and exports, the security settings and the pending consent prompts).
- [x] Render the security in the sidepanel (the Security section with the denydefault allowlist editor, the origin profile editor, the consent windows with the remaining time and renew action, the consent prompts with their class badges and the duration choice, the revoke confirm dialog naming the halted steps, the mask rules with the redacted field notes, the revocation history and the per run chain verification with the seal hash, the verified log read and the audit file export) and the popup (the origin allowlist state with the grant request for new origins, the originprofile summary, the consent window countdown and the denydefault notice).
- [x] Extend the vitest suite to 1052 tests covering the denydefault refusals for ungranted origins, the exact origin matching with no wildcard expansion, the per profile workspace scoping, the originprofile grants and denials per kind, the sensitive classification and the option refinement with the default sensitive grades and the credential bearing submits, the fresh class consents per origin, the consentwindow expiry mid run with the suspension and the explicit renewal, the revokerun halting pending and queued steps, the hash chain across append, seal and read, the tamper detection on a broken chain link, the maskinputs across log and observation paths, the sensitive class routing through the consent gate, the security persistence with the verified log export and retention, the denied and revoked progress evidence and the three envelopes, all on plain fixtures; document the consent model in docs/15.consentmodel.md, the allowlists and originprofiles in docs 01, the security model in docs 03, the security arithmetic in docs 05 and the security surface flow map in docs 07, and promote 1.1.61 with synchronized metadata.

### 1.1.59 multi agent part two

- [x] Add the orchestration contracts to types.ts (the leaderworker topology with worker assignments, workerassignment, criticreview with verdict, issues and required changes, verifiercheck with claim, method and pass or fail outcome, the plannerexecutor split, handoffrequest and handoffrecord, resourcelock keyed by origin and selector with kind and expiry, conflictscan with overlapping targets and writers, mergeentry and resultreport with provenance, the progressboard layout with lanes and milestones, reviewrequest, escalationrecord, arbitrationrule, consensusround, swarmaction, swarmcost and outputcomparison) plus the swarmquorum, swarmworkers, boardretention and verifiermethods run settings and the critic and verifier roles in the agentrole union with read side defaults.
- [x] Implement the orchestration module (electleader by the user rule, assignwork slicing tasks across workers, collectresults with pending status, scaleworkers by load under the user bound with no engine cap, the planner executor split with step reports, requestreview, ackreview, applyreview and sweepreviews with ack and timeout, checkclaim with pass and fail outcomes, boardstate aggregation, escalate and resolveescalation, arbitrate by priority, age and leader, and the consensus rounds with quorum) and the coordination module (preparehandoff, transferhandoff under the one agent per tab rule, resumehandoff from the packaged state, acquirelock, releaselock and expirelocks, scanconflicts with suggested ordering, mergeresults under the first, last, preferagent and fail rules with provenance, swarmreport, compareoutputs, interleavetimeline, sharelesson, swarmcosts and replayagentrun).
- [x] Add the ten orchestration gates to policy.ts (leaderelectionvalid, criticreviewgrade as read only, verifiermethodgrade against the allowed methods, handoffgrantgate preserving the session grants, lockscopevalid inside one origin, conflictresolutiongrade marking overwrites sensitive, escalationgate as always human decided, consensusquorumvalid, workerscalevalid with no engine cap and mergeegressgrade of page content exports), the boardstate, handoff and review envelopes with the leader worker, handoff, lock, conflict and merge grammar in protocol.ts, and the persistence of the topology, splits, reviews, verifier checks, review requests, handoffs, locks, conflicts, report, board snapshots, escalations, consensus rounds, timeline and costs in memory.ts with the getswarmtimeline accessor.
- [x] Execute the orchestration in the background behind the same consent gates: the swarmleader handler (elect, assign, collect, scale, split, stepreport, milestone), the swarmreview handler (request, ack, apply, sweep, verify, escalate, decide, consensus, vote), the swarmhandoff handler (prepare, transfer behind the grant gate, resume), the swarmlocks handler (acquire behind the scope validation, release, sweep, scan, arbitrate) and the swarmmerge handler (merge, report, export behind the egress grade, compare, lesson, costs, timeline, replay, snapshot), with every elect, assign, review, verify, handoff, lock and merge event landing its swarm audit entry and every orchestrated action appending to the interleaved timeline.
- [x] Render the orchestration in the sidepanel (the topology box with election, assignment, scaling and split controls, the progressboard with milestones, the review cards with critic verdicts and verifier badges, the escalation inbox with user decisions, the consensus view with votes and quorum, the handoff log with transfer and resume, the lock view with expiry countdowns and releases, the conflict scans with the suggested ordering, the merged report preview with its export, the compare, lesson and cost controls, the interleaved timeline with replay) and the popup with the elected leader and the held lock count.
- [x] Extend the vitest suite to 969 tests covering leader election by the first and named rules, work slicing, result collection, worker scaling under the bound, the planner executor split reporting, the critic verdict flows with ack and timeout, the verifier pass and fail checks, the progressboard aggregation, escalation, arbitration and consensus quorum math, the handoff prepare, transfer and resume, the lock acquire, release and expiry, the conflict scan detection, the result merging under every rule with provenance, comparison, timeline interleave, lessons, costs, replay, the ten gates, the three envelopes and the new persistence with the getswarmtimeline filters, all on plain fixtures; document the orchestration in docs/multiagent.md, the orchestration model in docs 03, the orchestration arithmetic in docs 05 and the orchestration surface flow map in docs 07, and promote 1.1.59 with synchronized metadata.

### 1.1.62 security part two

- [x] Introduce the secretvault records with scope, label and provenance behind a vault seam that persists metadata only, refuse secrets in step options, variables and plan texts through the digest leak scan and the masked shape carry check, resolve vault id markers at the last possible moment, and keep typed credential values out of every log, memory record and export.
- [x] Introduce the redactshot regions with geometry and reason, derive them from field shapes and user drawn masks, apply them to viewport, element and stitched captures alike before storage, and mark the stored records with the redaction evidence.
- [x] Introduce the schemastrict errors with path and expected shape, validate every inbound command before dispatch, reject unknown fields of the registered families, and record the rejections with no payload echo.
- [x] Introduce the origincheck verdicts for inbound messages and port connections, the connectallow entries for external senders that ship empty by default, and the drops of senders absent from the list without handler execution.
- [x] Introduce the ratelimit buckets per origin and per session with user configured bounds and windows, defer commands past their bound until the window resets, and record the deferred events with their reset times.
- [x] Introduce the confirmpay, confirmdelete and confirmcreds gate descriptors with their payloads (amount, payee origin and target for payments; target, scope and irreversibility for deletions; credential label only for credentials), require one distinct human action per gated step with no batch approval and no timeout, record the gate opens, resolutions and phishguard denies in the immutable log, and record the gate waits beside the step durations.
- [x] Introduce the phishguard verdicts with distance score and matched origin, run the guard before credential steps on new origins under the user threshold, expire the verdicts past the freshness window, apply safedefaults (reads only, every sensitive class denied) to origins without an originprofile, and record the applications with their first seen origins.
- [x] Mark the extracted markup untrusted before any render, route the untrusted renders through the sandboxframe, refuse the page context injection, compute the permdiff between two permission versions on each installed update, serve the transparency data in one read, and declare the transparencypage as the options surface that lists every grant, consent window, sender, permdiff and vault label with revoke actions.
- [x] Extend the vitest suite to 1130 tests covering the schemastrict rejections with path and expected shape, the origincheck on the message and port paths, the connectallow drops for unknown senders, the ratelimit buckets with deferral and reset, the confirmpay, confirmdelete and confirmcreds gates with the no batch and no timeout refusal paths, the secretvault flows with no plaintext persistence, the redactshots across every capture kind, the phishguard verdicts at the threshold boundary, the safedefaults application to unknown origins and the permdiff computation between two permission sets, all on plain fixtures; document the security model in docs/16.securitymodel.md, the architecture in docs 03, the arithmetic in docs 05, the surface flows in docs 07 and the consent model extension in docs/15, and promote 1.1.62 with synchronized metadata.

### 1.1.63 session interface

- [x] Introduce the session interface contracts in types.ts (sitenote records per origin with sealed sensitive bodies, scratchpad entries per task with step provenance, runsummary records with origins, kinds and per step outcomes, semanticrecall queries, index entries and matches, correctionmemory entries, consentmemory entries per origin, sessiongrid rows, historysearch queries and hits, errorsurface payloads with cause classes and retry hints, the cancelrun action with its rollback option descriptor, emptystate surfaces and per tab session references) beside the settings noteretention, scratchpadretention, summaryretention, correctionretention, recallwindow, historyindex, cancelrollback and summarywindow with the action kinds, permissions and manifest key untouched.
- [x] Implement the sessioninterface module (the note builders with the local at rest sealing, the append only scratchpad, the run summary distillation inside the user window, the fingerprint indexed recall corpus with run scoped text similarity ranking, the correction and consent memory records with the advisory verdicts, the sessiongrid row derivation with no new state, the incremental history corpus with matched term highlighting, the errorsurface classification and retry hints, the cancelrun rollback of queued steps only, the per tab isolation references, the empty state guidance and the audit bundle export) and the runsummary offscreen worker task in environments.ts.
- [x] Add the ten session interface gates to policy.ts (sitenotesreadgate for granted origins, sitenoteswritegate behind explicit consent, scratchpadscopegate for the owning task session, memoryreadscopegate limiting correction and consent memory reads to planning and prompting, semanticrecallscopegate refusing origins outside the run scope, summarywindowvalid and sessionretentionvalid with no engine caps, consentmemoryadvisorygate keeping every record advisory with denials weighing the same as grants, cancelrungate for the queued only rollback and retrydispatchgate for the reviewed retry).
- [x] Persist the five session stores in memory.ts (sitenotes with read, write, list and expiry per origin, the scratchpad with append, read and prune per task, runsummaries with write and list per origin, the recall index with fingerprint deduplication and the user window, corrections and consentmemory with record and query per origin, errorsurfaces, the incremental historysearch corpus, the per tab session references with the lock state listing and the one audit bundle export) through the adapter seam documented for the future remote recall backend.
- [x] Execute the session interface in the background behind the same consent chain (the sessions command family with schemastrict grammar, the run summary distillation at run completion as an offscreen worker task with inline fallback, the scratchpad appends and recall index entries beside every step outcome, the errorsurface record with the retry hint in the response envelope, the correction capture from plan rejections and replan approvals, the consent memory records beside every grant, denial, expiry and revocation, the cancelrun with the queued only rollback that leaves the sealed log untouched, the reviewed retry dispatch and the sessionview report beside the context sessionpreferences).
- [x] Render the session interface in the sidepanel (the session grid with resume, cancelrun, reopen and deep link actions, the history search box with highlighted hits, the inline site notes editor with the sensitive sealing, the live scratchpad, the session memory section with run summaries, semantic recall, corrections, consent decisions and error surfaces with retry buttons), the popup (the compact session grid with deep links to the sidepanel run view) and the transparencypage (the session interface options with the recall window, the four retention windows, the summary window, the historysearch toggle and the cancelrun rollback preference).
- [x] Extend the vitest suite to 1161 tests covering the site notes with sealing and expiry, the scratchpad with pruning, the run summary distillation and worker request, the recall ranking and provenance, the correction capture and matching, the consent memory decisions and advisory verdicts, the session grid derivation, the history search filtering and highlighting, the error surfaces with retry hints, the cancelrun rollback, the per tab isolation, the empty states, the gates and the memory accessors with the bundle export, all on plain fixtures; document the five session stores in docs/17.memorymodel.md, the architecture in docs 03, the arithmetic in docs 05, the surface flows in docs 07 and promote 1.1.63 with synchronized metadata.

### 1.1.64 interface surfaces part one

- [x] Introduce the interface surface contracts in types.ts (the five uisurface kinds, commandpalette entries with id, label, keywords and action, taskinput submissions with text, context and origin scope, stepstimeline nodes with status, duration and environment, logstream events with level, source and step ref, plancards with kind, risk class, environment and options, stepapprove resolutions with approve, reject and edit, diffpreview payloads with before and after states, onboarding steps with surface, body and completion event, surface actions, bus routing verdicts, broadcast frames and channels, surface layout preferences and palette usage records) beside the settings paletterecents, logstreambuffer, taskinputretention, paletteshortcut and diffpreviewbytes with the action kinds, permissions and manifest key untouched.
- [x] Implement the surfaces module (the palette catalog registered from every module at startup, the capability listing, the fuzzy ranking with the recent commands first, the taskinput submissions and history, the onboarding walkthrough with its single consent scoped completion event and on demand replay, the command bus routing through the policy gates and the broadcast frame builders with the kind to channel mapping) and the planreview module (the plancards with their risk groups and corrections, the stepapprove resolutions with their immutable log events, the diffpreview comparison with mask verdicts, the stepstimeline node derivation from progress with no new state, and the logstream with its hash chain, filters, bounded live window, live verification and verified audit excerpts).
- [x] Add the eight interface surface gates to policy.ts (paletteactiongate behind the existing permission gates, taskinputproposalgate through the same proposal flow as the api, planreviewgate requiring the plancard review before any execution, stepapprovegate binding one step per distinct human action with no batch and no background provenance, diffpreviewgate limiting generation to write class steps, onboardingconsentgate allowing exactly one consent scoped event, logbufferboundvalid and logstreamegressgate for the verified range copy) and the surfacesnapshot protocol envelope.
- [x] Persist the interface surface stores in memory.ts (the palette usage counts, the taskinput history per profile, the onboarding completion state, the per surface layout preferences, the logstream filter preferences and the stepapprove resolution history per origin) scoped per profile workspace, and serve the surface preferences beside the session preferences in the context response.
- [x] Execute the interface surfaces in the background behind the same consent chain (the surface command family with the schemastrict grammar, the command bus that routes every surface action through the policy gates with each routing audited, the single devthinksurfaces broadcast channel with one frame per audited event, the in memory logstream history behind its hash chain, the onboarding start on first install and its single consent scoped completion event, the taskinput submissions through the same proposal flow as the api with the active origin and page outline attached, the stepapprove resolutions written into the immutable run log with the correction capture, and the diffpreview offscreen offload above the user byte ceiling).
- [x] Render the five surfaces (the popup command surface with the taskinput, the quick run actions, the palette with its keyboard shortcut, the allowlist and environment status and the onboarding card; the sidepanel workspace with plan, run and review tabs hosting the taskinput box, the plancards with stepapprove and diffpreview, the stepstimeline and the live logstream; the dashboardpage full page view aggregating the sessiongrid, historysearch, notes, transparency views and onboarding state, opened from the popup and the sidepanel and through the new tab chrome url override; the optionspage gathering every setting in one page with the embedded transparency sections and the onboarding replay; and the manifest optionspage entry with the dashboardpage chrome url, both without new permissions).
- [x] Extend the vitest suite to 1192 tests covering the palette registration, capability listing, fuzzy ranking and usage recording, the taskinput submission and history flows, the onboarding sequencing, single consent event and replay, the command bus routing through the policy gates, the broadcast frames and channels, the plancard grouping with corrections, the stepapprove resolution recording with its log events, the diffpreview before and after comparison with mask verdicts, the stepstimeline node derivation from progress, the logstream filtering, chain verification, buffer bounds and verified excerpts, the manifest validation of the new chrome urls and the new gates, all on plain fixtures; document the five surfaces in docs/18.interfacesurfaces.md, the architecture in docs 03, the arithmetic in docs 05, the surface flows in docs 07 and promote 1.1.64 with synchronized metadata.

### 1.1.65 interface surfaces part two

- [x] Introduce the finishing interface contracts in types.ts (the datagrid views with columns, rows and inferred types; the exportmenu descriptors with format, scope and destination; the quickactions bound to context menu entries; the shortcutkeys bindings; the omniboxtask submissions; the statusbadge states; the notifydone and notifyattention payloads with deep links; the recenttray entries; the pickeroverlay sessions; the targethalo geometry; the guidedtips; the shotpanel views; the compareviewer pairs; the siteprofiles; the darklight tokens; the locale bundles; the importexport payloads; the dropimport sessions; the featuretour stops; the a11ylabels; the pagechips and the stetoasts) with the settings additions (recenttraydepth, notifyconsent, notifyenabled, themepreference, uilanguage, toastlivecount) and the audit kinds of the views family.
- [x] Gate the finishing elements in policy.ts (quickactiongate behind the origin allowlist of the clicked tab, omniboxtaskgate through the proposal and review flow, shortcutkeygate binding only palette commands with their gates intact, notificationcontentgate requiring consent before any page content shows, pickeroverlaygate limiting reads to the granted origin, shotpanelgate limiting views to captures of granted origins, siteprofilegate keeping per site preferences on https origins and importexportgate refusing secretvault values and unmasked logs under any flag).
- [x] Ship the finishing logic in eight pure modules (datagrid.ts with the inferred types, local sort and filter, row range selection and masked exports; quickactions.ts with the context menu family, the editable shortcuts and the omnibox parser; statusviews.ts with the badge states, notifications, recenttray and stetoasts; pickerviews.ts with the picker sessions, halo, tips and pagechips; evidenceviews.ts with the shotpanel and compareviewer; siteprefs.ts with the siteprofiles, darklight tokens and locale bundles; portability.ts with the importexport bundles and dropimport detection; tourviews.ts with the featuretour stops and a11ylabels) beside the interfaceviews protocol envelope.
- [x] Persist the finishing stores in memory.ts (the siteprofiles per origin, the shortcutkeys bindings and theme preference per profile, the recenttray entries with the configurable depth and the notification consent, preference and history per profile) and serve the finishing options in the context response.
- [x] Execute the finishing family in the background behind the same consent chain (the views command family with the schemastrict grammar, the statusbadge riding the declared action entry with updates on every run state audit, the notification payloads with their deep links behind the content consent and the do not disturb respect, the pagechip resolutions written into the immutable run log exactly like stepapprove, and the importexport bundles whose validation refuses secrets and unmasked logs in full).
- [x] Render the finishing elements (the popup recenttray with resume and reopen and the quickaction list behind the origin allowlist; the sidepanel data tab with the datagrid preview, the local filter, the masked export, the compareviewer, the shotpanel and the picker session; the dashboardpage dropimport zone; and the optionspage finishing options with the theme preference, the interface language, the tray depth, the toast live count, the notification consent, the shortcut editor, the importexport section and the featuretour replay with the guidedtip recall).
- [x] Extend the vitest suite to 1229 tests covering the datagrid type inference and partial export scope, the exportmenu formats with masking verdicts, the quickaction gating per active origin, the omniboxtask parsing into the proposal flow, the statusbadge states and attention counts, the compareviewer pairing and pagechip resolution, the importexport exclusions of secrets and unmasked logs, the locale fallback and formatting, the picker sessions with stability scoring, the shotpanel gating, the siteprofiles and darklight resolution, the dropimport detection, the featuretour stops and the localized a11ylabels, all on plain fixtures; document every surface element with its a11ylabel in docs/19.interfaceinventory.md, the finishing elements in docs/18.interfacesurfaces.md, the architecture in docs 03, the arithmetic in docs 05, the flows in docs 07 and promote 1.1.65 with synchronized metadata.

### 1.1.71 state depth

- [x] Introduce the state depth contracts in types.ts (the urlvisit with its url, title, time and runid, the runtimelineevent pairing a timestamp with a step, audit or visit source, the memoryprovenance of origin, runid, stepid and capture time, the expiryrule pairing a memory key pattern with its user lifetime, the quotareport with usage, quota, remaining and ranked candidates, the tabstate mapping a tabid to its isolated namespace, the lockrecord with holder, runid, acquiredat and expiresat, the auditexportrecord bundling runs, memory, expiry rules, timeline and locks, and the memoryitem wrapping a stored value with its provenance and encryption flag) beside the agentsession lockid, the runrecord visits and the lockwindow, expireinterval, expireauditevents and encryptrest settings, and grow the auditkind with the lock, purge and visit events.
- [x] Add the runhistory module (visitof, appendvisit with the consecutive duplicate folding, stepeventof, auditeventof, visiteventof, runtimelineof, buckettimeline, tabnamespace, tabkey, tabisolate, acquiresessionlock, lockblocks, releasesessionlock, lockwindowof, stalelocks, sessionwithlock, visitsummary, finalurlof and runwithvisits) and the memorycare module (provenanceof, memoryitemof, attachprovenance, stepsummaryfor, matchingrule, expiryof, expireditems, purgeitems, quotareportof, rankedcandidates, cleanupbatch, bytesreclaimed, auditexportof, exportchunks, derivekey, encryptvalue, decryptvalue, migrateitem, issensitiveclass and exportready), both exported through the library index.
- [x] Gate the state depth in policy.ts (the sessionlockgate that blocks a concurrent run while naming its holder, the tabisolategate that refuses a step outside the isolated namespace, the urlhistorygate that confines the capture to the approved origin, the timelinereadonlygate, the expirygate that requires the confirmation before purging, the encryptmemorygate that refuses the plaintext writes of the sensitive classes, the quotacleanupgate that requires the per batch approval, the auditexportgate, the lockwindowvalid, the encryptionsecretgate that treats the secret entry as a consent prompt, the urlhistoryscopegate that never merges runs and the exportprovenancegate) and extend protocol.ts (the tab namespace and the observation provenance of the request body, the runtimelinereport schema, the memoryitemframe that rejects payloads without provenance, the auditexportreport grammar, the lockid preservation across replays and the urlvisitscheck against the session origin).
- [x] Persist the state depth through memory.ts (getvisits and setvisits per run, getruntimeline, setruntimeline and appendtimelineevent, getlock, setlock, clearlock and listlocks, gettabstate and settabstate, getmemoryitems, setmemoryitem and removememoryitems, getexpiry and setexpiry, getquotareport and setquotareport, getencryptrest and setencryptrest, addpurgesummary and listpurgesummaries, getlastexpirepass and setlastexpirepass, and getexport building the auditexportrecord on demand) with the documented encrypted backend adapter seam.
- [x] Wire the background (the sessionlock acquisition before the first step with its release on completion, failure and cancel, one urlvisit per completed navigation step with the final url after redirects, one timeline event per step result, the provenance wrapped observations, the quotawatch pass after each completed run, the expirememory interval pass, the stale lock expiry at startup before the zombiecheck, the auditexport through the download flow and the state command family with its audits) and the pagebridge (the final url report of the page and the waiturl finalurl detail).
- [x] Render the surfaces (the sidepanel state depth section with the runtimeline phase buckets, the urlhistory summary, the lock holder naming, the quotareport with its cleanup control, the expiryrules editor and the audit bundle export; the popup encryption status; and the style marks for the encrypted, expired and isolated items).
- [x] Cover everything with tests/runhistory.test.ts and tests/memorycare.test.ts on plain fixtures (the urlhistory append, dedupe and per run isolation, the runtimeline ordering across steps, visits and audit events, the sessionlock acquire, block, release and expiry with the tabisolate namespaces, the provenance attachment and the auditexport completeness, the expirememory rules and the quotawatch cleanup batches, the encryptrest roundtrip and the lazy migration, the gates and the protocol grammar) and document the state depth arithmetic in docs/05.agentalgorithms.md, the architecture in docs/03, the flows in docs/07, the changelog paragraph, the README row, the releasegates count and promote 1.1.71 with synchronized metadata.

### 1.1.70 resilience

- [x] Introduce the run lifecycle contracts in types.ts (the runstate values queued, running, paused, awaitingapproval, completed, failed, cancelled and rolledback, the runrecord, the checkpointrecord with its completed steps and page digest, the rollbackitem with its human readable compensation, the queuedtask with its monotonic sequence, the heartbeatrecord, the runtransition, the toolstep idempotencykey, the planprogress runid and the observation digest) and grow the auditkind with the queue, checkpoint, rollback and reap events.
- [x] Add the resilience module (runstatemachine, advancerun, initialrun, stepkey, the offlinequeue family with enqueueoffline, dequeueoffline and replayqueue, the checkpoint family with makecheckpoint, resumecheckpoint and checkpointisvalid, the heartbeat family with runheartbeat, heartbeatisstale and heartbeatwindowof, zombiecheck and reaprun, sideeffects and rollbackrun with its per kind compensation table, cancelrunrecord and cancelrollback, runfail, runcomplete, runfromplan and runsummaryof) and export every function through the library index.
- [x] Gate the lifecycle in policy.ts (the canexecute refusals of queued, cancelled and rolledback runs and the fresh heartbeat requirement of sensitive steps, the replaycheck, the checkpointgate, the rollbackgate, the zombiegate, the rollbackorigingate, the heartbeatwindowvalid, the offlinegate, the queuedtaskexpirygate and the queuedepthvalid) and extend protocol.ts (the resumedfrom marker with its idempotency collision refusal, the runstate and queuedepth of the request body, the runid and runstate of the response envelope, the queue and replay grammar and the rollbacksummarycheck).
- [x] Persist the resilience state through memory.ts (getrunrecord and setrunrecord, listruns by recency, the latest checkpoint per run, the offlinequeue with restart survival, the heartbeat per run, the rollback items, the executed idempotencykeys per run, the user heartbeat window and queue depth and the failed run retention with pruneruns) with the documented disk backed queue adapter seam.
- [x] Wire the background (the runrecord at approval, the offlinequeue enqueue and online replay, the heartbeat before each step, the checkpoint after each sensitive step, the executed key recording, the runcompletion broadcast, the rollback offer of a failed run, the zombiecheck and pruning at every service worker wake, the resilience command family with its audits and the run summary report) and the pagebridge (the idempotencykey execution with the replay skip, the pagedigest and the checkpointafterstep payload).
- [x] Render the surfaces (the sidepanel runstate badge beside the plan with the checkpoint resume control, the offline queue depth with the replay now control, the zombie runs with their reap timestamps, the rollback or plain cancel choices and the executed idempotencykeys, the popup heartbeat staleness of long tasks and the runstate colors in style.css) and cover everything with tests/resilience.test.ts on plain fixtures (every legal and illegal transition, the checkpoint lifecycle, the per kind compensations, the zombie staleness and reaping, the queue order and expiry, the idempotent replay deduplication, the canexecute runstate gates, the protocol markers and the memory persistence).
- [x] Document the run lifecycle state machine in docs/05.agentalgorithms.md, the architecture in docs/03, the flows in docs/07, the changelog paragraph, the README row, the releasegates count and promote 1.1.70 with synchronized metadata.

### 1.1.69 performance part two

- [x] Introduce the scheduling contracts in types.ts (the backpressure signals, the domain lanes with per domain concurrency slots, the politedelay profiles with jitter and floors, the adaptivepoll windows with floor, ceiling and growth, the coalesced requests with their waiters, the runbudget records, the budgetalerts with severity levels, the timeoutcancel policies and events, the tabsuspend states, the runcache entries keyed by digest, the stepprefetch hints, the durationmeter samples, the selectorprofile stats, the steptrace spans, the startupmeter samples, the batteryaware states, the networkaware retry rules, the readparallel groups, the warmselector records, the cadence controls, the slowmo sessions, the resume checkpoints, the navdedupe entries, the sessionreuse grants, the artifactcompress records and the logprune rules) beside the twenty two settings additions (batchwindow, domainlimits, politedelay, politejitter, pollgrowth, stepbudget, memorybudget, budgetthresholds, timeoutbound, suspendwindow, startuptarget, selectorlatency, slowmofactor, runcachepin, logpruneretention, logsizewindow, artifactcodec, batteryfloor, netbackoff, readlanes, cadencewidening and the perf family carry over) and the sixteen audit kinds of the schedule family with the action kinds, permissions and manifest key untouched.
- [x] Gate the scheduling elements in policy.ts (batchwindowvalid, domainlimitsvalid, politedelayvalid, adaptivepollvalid, runbudgetvalid, budgetthresholdsvalid, timeoutboundvalid and suspendwindowvalid bounding every window, limit, threshold and factor by user choice only, timeoutrecordeventgate requiring the immutable log cancel event beside the abort, sessionreusegate behind the explicit per profile consent prompt, resumefingerprintgate refusing a changed page, logprunegate keeping the prune to whole sealed runs only, stepprefetchgate refusing any hint outside the reviewed plan and slowmofactorvalid keeping the factor a positive user choice).
- [x] Ship the scheduling logic in six pure modules (batchscheduling.ts, runbudget.ts, stepmeter.ts, runresume.ts, resourceaware.ts, runparallel.ts) beside the schedulereport protocol envelope.
- [x] Persist the scheduling stores in memory.ts (the domain lanes per run, the runbudget records, the budgetalerts, the timeoutcancel events, the tabsuspend states per run, the runcache entries with the run end sweep that keeps the pinned entries, the resume checkpoints per run, the selectorprofile stats, the steptrace spans per run, the startupmeter samples, the slowmo sessions, the sessionreuse grants and the artifactcompress records) and serve the scheduling preferences in the context response.
- [x] Instrument the executor and the command family in background.ts (the schedule command family with its schemastrict grammar covering backpressure, domain, polite, poll, coalesce, budget, alerts, timeout, suspend, cache, prefetch, resume, navdedupe, sessionreuse, compress, logprune, startup, battery, network, parallel, warm, slowmo, settings and view; the navdedupe skip of the already active url, the timeout race with its cancel event in the immutable log, the suspend plan on long waits with the restore before the next step, the runcache serve and store on GET fetch steps with the run end sweep, the durationmeter writing monotonic samples into the perf records, the selectorprofile and steptrace recording beside every step outcome, the runbudget evaluation with the critical pause through the session pause, the resume checkpoints at the step boundaries and the startupmeter sample at the service worker ready point).
- [x] Render the scheduling surfaces (the optionspage scheduling and budgets section with every window, limit, delay, budget, threshold, bound, target, factor, codec and pin; the sidepanel batch lanes section with the domain slots, the queued overflow, the backpressure state and the timeoutcancel events with their retry hints; the dashboardpage budgetalert view with its thresholds and the selectorprofile latency chart; and the schedule report envelope serving all three).
- [x] Extend the vitest suite to 1418 tests covering the batch backpressure pausing without dropping queued steps, the domain lanes with their slots and overflow, the politedelay spacing above the floors, the adaptivepoll widening and narrowing, the requestcoalesce merging and fanout, the cadence control under memory pressure, the runbudget tracking, the budgetalert severities with the critical pause, the timeoutcancel decisions with the immutable log event gate, the efficientresume checkpoints, skipping and fingerprint revalidation, the navdedupe skip and folding, the runcache digest keys, serving, sweeping and pinning, the stepprefetch hints behind the plan structure gate, the sessionreuse consent grants with the per task containers, the durationmeter samples written into the perf records, the selectorprofile statistics and flags, the steptrace nesting and trace file export, the startupmeter and coldstart verdicts, the slowmo sessions with their pause linking, the tabsuspend plans and restores, the artifactcompress codecs with lazy reads, the logprune of whole sealed runs only, the batteryaware deferrals, the networkaware backoffs with the server signal override, the readparallel lanes and the warmselector carries, all on plain fixtures; document the scheduling model in docs/23.schedulingandbudgets.md, the architecture in docs 03, the arithmetic in docs 05, the flows in docs 07 and promote 1.1.69 with synchronized metadata.

### 1.1.68 performance part one

- [x] Introduce the performance contracts in types.ts (the lazymods descriptors with module id, load reason and capability requirements ahead of load; the lazyload telemetry records; the startup module budget view; the debouncedom profiles and coalesced batches; the batchquery plans; the incrsnapshot deltas with base ref, change set and fingerprint; the selcache entries, invalidations and states; the virtlist windows with row range and height map; the streamparse tokens and chunks; the chunkextract cursors and window results; the perf records with duration, query counts, cache hits and provenance; the worker queue depth samples) beside the settings prewarmset, lazybudget, debouncewindows, snapshotcadence, workerdepth, perfretention, virtlistrows and capturepriority, the observation delta flag, the progress perf and delta records, and the nine audit kinds of the perf family with the action kinds, permissions and manifest key untouched.
- [x] Gate the performance elements in policy.ts (lazyloadgate keeping lazy loading transparent to the capability checks, debouncewindowvalid and virtlistwindowvalid and lazybudgetvalid bounding every window, depth and budget by user choice only, batchqueryplangate refusing unfolded duplicates, incrsnapshotgate requiring the same run base, selcachegate refusing stale generation hits, streamparsegate running every chunk through schemastrict, chunkextractgate verifying the table fingerprint on resume, workerbackpressuregate deferring past the user depth instead of refusing and perfprovenancegate attaching provenance to every perf record).
- [x] Ship the performance logic in seven pure modules (lazyload.ts, dombatch.ts, snapshotdelta.ts, selcache.ts, virtlist.ts, streamparse.ts, perfrecords.ts) beside the perfreport protocol envelope.
- [x] Persist the performance stores in memory.ts (the selcache states per run with the run end prune, the incrsnapshot base refs and deltas per run, the chunkextract cursors per table, the perf records per step under the user retention, the lazyload telemetry, the queue depth samples, the virtlist height maps per surface and the perf audit bundle export with the metrics backend seam) and serve the perf preferences in the context response.
- [x] Instrument the executor and the worker pool in background.ts (the perf command family with its schemastrict grammar, the parse offload recording perf records with turnaround and queue depth, the backpressure deferral that keeps the parse inline, the snapshot request dedupe within a run, the empty delta recomputation skip, the heavy capture deferral behind the user speed priority, the abandoned parse task cancellation on the mid run revocation halt, the parse durations on the stepstimeline nodes and the batchquery, streamparse and chunkextract worker tasks in the offscreen document).
- [x] Render the performance surfaces (the popup worker queue depth and startup budget, the sidepanel run footer with the query counts, cache hits and selcache generation beside the parse durations on the stepstimeline, the logstream, the datagrid with the chunk cursor progress line and the compareviewer through the virtlist window, the dashboardpage perf summary with the step duration chart, the optionspage performance section with the cadence, the debounce windows per event kind, the worker depth, the prewarm set, the startup budget, the perf retention, the virtlist row window and the capture priority, the onboarding performance tour stop and the featuretour chunked extraction stop).
- [x] Extend the vitest suite to 1374 tests covering the lazymods resolution and prewarm hooks, the debouncedom coalescing across event storms, the batchquery folding of repeated selectors, the incrsnapshot delta computation and the empty deltas, the selcache hit, invalidation and revalidation, the virtlist windowing and height maps, the streamparse progressive observations, the chunkextract windowing, resume and merge, the worker queue backpressure and the perf recording per step, all on plain fixtures; document the performance model in docs/22.performancemodel.md, the architecture in docs 03, the arithmetic in docs 05, the flows in docs 07 and promote 1.1.68 with synchronized metadata.
- [x] Ship the fleet registry of the 1.1.72 family (agentfleet.ts grouping agentname with the lowercase identifier rule and the reserved identities, agentscopeof intersecting the requested origins and action kinds with the session grants and the allowed catalog, readonlyscope for the observer agents, agentbudgetof granting from user configuration with no hardcoded cap, spendbudget spending one unit per executed step with refusal past the ceiling, budgetremaining reporting, pauseagent pausing one agent without stopping its peers, unpauseagent, engagekillswitch cancelling every agent run and clearing every queue with one stop entry per agent and fleetoverview for the panel).
- [x] Ship the agent review layer of the 1.1.72 family (agentreview.ts grouping escalationblock holding the raising agent until the human answers, reviewrecordof routing one agent output to a peer, recordverdict writing the verdict beside the untouched original output, runreplay capturing the ordered steps and results, reconstructreplay rebuilding from the audit trail with the reconstructed marker, outputcompare aligning two competing outputs field by field into matching, conflicting and missing, and consensusrecordof collecting one vote per agent under the user quorum with the dissent kept).
- [x] Gate the fleet in policy.ts (agentscopegate refusing steps outside the requesting agent scope with the read only refusal, budgetgate refusing past the user ceilings, escalateholdgate holding every escalation until the human answers, reviewrequestgate requiring both agents to share the origin grant, killswitchgate extended to refuse agent triggers and stay effective over paused agents, pauseagentgate naming the peers that stay runnable, agentnamevalid validating the lowercase identifier rule, fleetoperationgrade marking outputcompare and consensusvote read only, replayexportgate binding replay exports to the audit consent boundary and voteweightvalid keeping one vote per agentrecord).
- [x] Carry the fleet identity through the protocol (requestbody including the agent block of agentid, name and role beside the budgetstate, parseproposal attributing every step to its requesting agent and refusing agents missing from the registry, consensusreport carrying the vote records with the tally and the outcome in the versioned envelope, and the escalation and reviewrequest message grammars documented in the header).
- [x] Persist the fleet family in memory.ts (getagent, setagent, listagents, getbudget, getagentscope, getreviews, getreplays under the user replayretention, getcomparison, getvotes, the killswitch stamp and the paused agents beside the orchestration era escalation accessors, with the fleet seam documented for a reviewed shared registry backend).
- [x] Wire the fleet command family in background.ts (register through agentname before the first run with the scope intersection and the budget grant, the single agent pause, the user triggered killswitch clearing the swarm taskqueue and the offlinequeue with one audit event per stopped agent, the escalation answers, the review requests with their verdicts, the on demand outputcompare, the consensusvote with its quorum, the runreplay reconstruction, the executor gates with the scope, budget and escalation hold on every attributed step and the spend per executed step, the runrecord agentid attribution at approval, the runreplay capture at completion, the agent identity in the proposal request and the unknown agent refusal at the protocol boundary).
- [x] Render the fleet control surfaces (the sidepanel fleet section with the fleet list, the remaining budget per agent, the single agent pause from its row, the escalations with their answer controls, the reviews with their verdict controls, the outputcompare side by side and the vote tally with its outcome and dissent, the popup killswitch button beside Stop and the fleet state line, and the fleet badges in the style).
- [x] Extend the vitest suite with twelve fleet cases (agentname uniqueness with the scope narrowing, the budget grant with its spend refusal and reporting, the pause isolation with the killswitch completeness, the escalation blocking with the review routing, the runreplay capture with its audit reconstruction and the outputcompare alignment with the consensusvote tallies); document the fleet control and consensus arithmetic in docs/05.agentalgorithms.md and promote 1.1.72 with synchronized metadata.
- [x] 1.1.86: implement the browser coverage with the apimap module (the catalog of every webextension api the codebase touches with the chromium, firefox and safari equivalents, the runtime browser probe with the in memory cache, the build time unmapped api report that fails the build before the cross browser artifacts ship), the polyfill layer (the namespace resolver that prefers the browser namespace and falls back to the chrome namespace, the callback to promise normalizer, the sidepanel popup window fallback, the scripting tabs.executeScript fallback, the storage local and session area keys, the tabs and windows normalizers, the notifications, contextmenu, clipboard, downloads and runtime messaging shapes), the firefoxprep adapter (the browser specific settings with the generated extension id, the action key mapping, the event page background scripts, the optional permission names that differ, the empty host permissions the deny by default posture keeps, the background bundle split for the event page semantics, the deny list in force across overlays and the web accessible resources firefox pattern syntax), the xpipack assembler (the firefox build as a zip ready for signing with the manifest placement at the archive root, the hashed assets beside it, the artifact name that carries the release version and the addons linter markers the build asserts), the safariskeleton wrapper (the xcode project wrapper around the chromium extension payload, the app entitlements for the extension distribution, the minimal app shell that opens the extension, the popover equivalent for the sidepanel surface), the per browser overlays (the firefox overlay and the safari overlay layered on top of the source manifest with the csp overlay, the web accessible resources firefox pattern syntax and the cross browser invariants — the kind catalog, the policy gates, the observation schema, the audit trail format, the bridge pairing, the gateway adapters and the mcpmode run unchanged on every browser through the fetch seam, the native transport stays chromium first and reports unsupported elsewhere, the feature flags default to the intersection set across browsers, the version sync stamps the same version into every browser manifest, the structured errors with the retry hint none on the unsupported calls), the cross browser build additions to tests/build.mjs (the firefox target that emits the adapted bundle, the safari target that embeds the chromium payload, the per browser size budgets, the no underscore bundle naming, the checksums for the firefox and safari artifacts, the apimap build check that fails on any unmapped api, the package.json files list that includes the manifest overlays), the verify workflow (the firefox prep step offline, the addons linter over the xpipack output when installed, the firefox load smoke when available), the release workflow (the xpi and the safari skeleton as release assets beside the existing artifacts), the containerfile smoke (the firefox prep step offline), the workflowcheck that covers the firefox and safari jobs, the npmgate check that allows the cross browser build scripts in the tarball, the docs/18.browsercoverage.md with the apimap table, the per browser permission differences, the sidepanel fallback behavior and the signing and notarization path per browser, the updates to docs/01, 03, 08, 10, configuration, packagenaming, releasepipeline and the readme browser coverage section, the apimap tests, the polyfill tests, the firefoxprep manifest tests, the xpipack tests, the addons linter test, the safariskeleton tests, the cross browser policy and protocol tests, the sidepanel fallback tests, the feature flag tests, the bridge pairing cross browser tests and the version sync tests.
- [x] 1.1.86: wire the firefox and safari targets through the background and the build (the firefox target reads the source manifest, layers the firefox overlay through the firefoxprep adapter, assembles the xpi through the xpipack assembler with the adapted manifest and the hashed extension dist bundles, and writes the artifact beside the chromium extension zip with the addons linter markers inside the budget; the safari target embeds the chromium extension payload into the safari app extension wrapper through the safariskeleton module, and the wrapper ships beside the chromium zip and the firefox xpi with the bundle id, the entitlements and the minimal app shell; the apimap build check asserts every recorded api has a chromium and firefox mapping so a new api without a row fails the build before the cross browser artifacts ship), persist the firefox overlay manifest through the package.json files list, render the cross browser invariants in docs/18.browsercoverage.md, emit the apimap, browserpolyfills, firefoxprep, xpipack and safariskeleton bundles through the build with size accounting, the naming check and the declaration files, add the firefox prep step, the addons linter step and the firefox load smoke step to the verify workflow, add the xpi and the safari skeleton to the release workflow artifact chain, the containerfile smoke runs the firefox prep step offline, the workflowcheck covers the firefox and safari jobs, the npmgate allows the cross browser build scripts and the per browser overlay manifests in the tarball, and document the apimap table, the per browser permission differences, the sidepanel fallback behavior, the signing and notarization path per browser, the cross browser invariants and the feature flag intersection set in docs/18.browsercoverage.md with the updates to docs/01, 03, 08, 10, configuration, packagenaming, releasepipeline and the readme browser coverage section.
- [x] 1.1.87: implement the publishing pipeline with the nugetpack module (the content files for the cli, headless and mcp entries, the umd and cjs bundles as content assets, the declaration files for ide integration, the framework targets matching the csproj profile, the project url, license and readme in the manifest, the fixtures directory as sample content and the full layout the checked-in csproj mirrors), the mavenpack module (the module per consumption mode with the library, cli, headless and mcp jars, the bundles embedded as resources inside each jar, the declaration files attached as classifier artifacts beside the extension zip, the pom metadata and license, and the module pom texts the checked-in poms mirror), the containerpack module (the multi stage build with the builder validation chain and the cli manifest and headless smoke as build checks, the runtime surfaces — the static site, the socket relay and the mcp server — behind the operator chosen binds, ports and paths, the image tags with the version beside the stable channel alias and the pre suffix for prereleases, and the digest files with the image hash), the vsixpack module (the pure zip based vsix archive with the extension manifest at the root and the package manifest, extension host, webview page and library esm build inside the extension folder, the chatbridge surface in the webview panel with the consent gates inside the webview ui, the user configured relay url with the empty default, the telemetry off and no network default statements, and the marketplace metadata check that validates the package fields without naming any vendor endpoint), the sbom module (the cyclonedx inventory of every artifact with its hash, size and channels, the deterministic no timestamp rendering and the coverage check), the artifactmanifest module (the manifest document with names, sizes, sha256 checksums and channels, the channel resolution of every release artifact name, the built set check that catches missing, mismatched and unexpected files, and the rollback pin of the previous release artifact set through its immutable tag) and the relayserve module (the socket relay state machine speaking the servercontract for self hosting — the session create and join, the pairing code exchange, the per frame token authentication with rotation and revocation, the event routing between the extension and the site members, the memory only token issuance and the idle sweep inside the operator chosen window).
- [x] 1.1.87: wire the publishing pipeline through the build, the workflows and the release chain (the build emits the vsix, the site zip, the declarations zip, the local artifact manifest and the seven publishing module bundles with their budgets, naming check, sourcemaps, minified variants, declaration files and checksums; the release workflow publishes a package for every mode in one chain with the channel jobs gated on the assemble job — npm, GitHub Packages, maven with the module jars, nuget with the richer distribution, container with the stable alias, digest, vsix, firefox and site — each publish retrying inside the bounded three attempt policy, the registry jobs and the github release requiring the release approval environment protection, the sbom job emitting the cyclonedx inventory over every release artifact, the attestation job creating the provenance attestations after every channel job through the workflow identity with no signing key material in the repository, the releaseassets job emitting the artifact manifest with names, sizes, checksums and channels beside the checksums file, and the github release gathering everything into a draft whose verification step downloads every asset and checks the checksums before publication), the version sync step stamping the same number into package.json, the extension manifest, the pom, the csproj, the vsix manifest, the xpi overlay manifest, the safari overlay manifest, the maven module poms and the version catalog of docs/runtimeversions.md with the check mode verifying zero drift across every packaging file, the release notes generator listing the per channel artifact sections, the verify workflow running the vsix package build, the nuget pack dry run, the maven pack dry run, the container image build with its smoke and the artifact manifest check against the built set, the maintain workflow updating the version catalog after each release, the npmgate allowing the publishing pipeline bundles in the tarball, the workflowcheck covering the new jobs and gates, the npm tarball growing the site source through the files allowlist with the exports map gaining the relayserve, sbom and artifactmanifest entries, and document the whole distribution in docs/19.distribution.md with the channel map, the package contents, the container usage, the vsix installation, the artifact manifest format, the retention, rollback and provenance posture, the walkthrough in dist/fixtures/selfhosted-container.md and the updates to the readme, the packagenaming, the releasepipeline, the artifact inventory, the transport matrix, the release gates, the configuration, the securitytriage and the contribution docs.

- [x] 1.1.90: merged the 94 correlated root modules into the 25 category families (run, flow, plan, session, swarm, net, bridge, gates, security, serve, capture, views, environments, runtime, perf, crossbrowser, workflow, llm, tools, data, commands, export, evidence, pack, debug) with the dist bundles, the exports map and the release chain following the families, and retired the 4 merged maven packages from the github packages registry
- [x] 1.1.91: froze the protocol contracts with the protocolv2 pin — the frozen message catalog with the ten schema files under docs/schemas (session, proposal, plan, observation, envelope, capability, audit, memory, progress, tool) each carrying the protocolv2 id and a frozen semantic version, the versioned envelope carriers, the stable error code table with the retry semantics of every code, the stdio and http framing rules, the immutable action kind identifiers and the reserved fields map of the observation payload — plus the seven capability manifests under caps (background, pagebridge, sidepanel, popup, cli, library, mcp) with the message types, kinds, permissions and per tool versions each surface pins, the capmanifest serving on the background capmanifest message and the describe command of the cli, the capmanifest drift comparison, the deprecation registry with the sunset releases and the one warning per session rule, the protocol major negotiation (two for new clients, one with the deprecation notice, refusal with the supported range above two), the strict unknown field refusal for version two against the tolerant version one window, and the tests/apifreeze.mjs gate wired into the validate chain and the verify workflow with the freeze artifact tests/artifacts/apifreeze.json hashing every frozen contract
- [x] 1.1.95: implement the security hardening release (the roadmap 1.1.94 section landed as 1.1.95 after the real 1.1.94 shipped the single root manifest consolidation) — the pentest checklist of tests/pentest.mjs executing all twenty one automated entries against the real compiled modules of dist (the consent gate blocking a sensitive kind on a fresh profile, the origin check rejecting cross origin messages, the connect allowlist refusing unknown callers, the schema validation rejecting malformed envelopes, the escape hatch stopping a running plan from any state, the kill switch stopping all agents at once, the revoked consent aborting the in flight step, the rate limit throttling command floods, the port allocation refusing remote bindings, the auth handshake rejecting unpaired clients, the session tokens expiring on their timer, the secret store never leaking into the audit trail, the loghash chain detecting tampering, the sandbox isolating untrusted content, the quarantine holding downloads until scanning, the cookie jar isolated per run, the phishguard warning on lookalikes, the payment gate refusing every non human resolution, the delete gate requiring the human choice, the purge removing every trace of a run and the export returning all stored data in one archive) with the deterministic artifact tests/artifacts/pentest.json, the manual checklist with its human verification steps in docs/pentest.md, the consent requirement coverage of the pool items 545 through 588, the cspaudit gate of tests/cspaudit.mjs extracting every content security policy from the root manifest and both browser overlays with the seven checks (no eval, no remote code, no unsafe sources, no wildcards, the pagebridge script the only injected file, the extension pages local only, the sandbox frame rendering the untrusted extracts), the permdiff gate of tests/permdiff.mjs comparing the permission set against the previous release artifact with the justification table requirement and the clean diff artifact tests/artifacts/permdiff.json, and the three gates wired into the package validate chain (validate:security), the verify workflow steps and the workflowcheck permitted set.
- [x] 1.1.95: ship the strict content security policy and the completed transparency page — the root manifest gains content_security_policy.extension_pages of script-src 'self'; object-src 'self'; frame-ancestors 'self' with both overlays byte identical so the extension pages refuse framing by remote origins, the web design templates carry no inline event handler anywhere, the transparencypage template gains the permissions section (every manifest permission with its consuming surface through the permissioncoverage of the capability manifests), the stored data kinds section (every inventory entry with its location, its purge link with the typed confirmation and the one archive export link), the audit integrity section (the loghash chain verification result of every stored run log), the grant dates on the consent session rows and the transparency export behind the masking and redaction pass, all rendered entirely offline with no external request; the hardening family of hardening.ts (masklogtext masking the secret shaped key value pairs of every free form log text the audit trail keeps, localscangate answering the local signature check of the virus scanning hook on the device before any remote endpoint fires, hatchbannerof building the visible escape hatch confirmation banner the options page renders, newdomainpausecard pausing the review mode on new domains with the explanatory card, runtracekeys selecting every stored key of a run trace, syncrotationgate gating the key rotation of the encrypted sync path, stdioclientsignaturegate verifying the client signature of the local stdio bridge when the platform allows it and transparencyexportmask masking and redacting the transparency exports) rides beside the frozen index surface as its own dist entry.
- [x] 1.1.95: document the security review — docs/pentest.md with the automated checklist table, the manual entries and the rerun instructions, docs/cspaudit.md with the policy set and the audit rules, docs/transparency.md with the page contents and the offline posture, docs/securityreview.md with the pentest execution, the findings with their fixes and verification, the cspaudit results per surface, the permdiff history since the 1.1.31 baseline, the residual risks with their owners and the go decision checklist gaining the security review entry, the permdiff justification table in docs/01.extensionpermissions.md, the security gate row in docs/releasegates.md, the security review link in SECURITY.md, the release candidate triage flow in docs/securitytriage.md, the roadmap progress line update, the migration note for upgraders in docs/migrationguide.md, the quarantine and scanning options in docs/configuration.md, the security artifact inventory in docs/referencecatalog.md and the readme transparency and security review note.
- [x] 1.1.96: certify the multi agent coordination layer (the roadmap 1.1.95 section landed as 1.1.96 after the real 1.1.95 shipped the security hardening) — the agentcert gate of tests/agentcert.mjs executing all thirty coordination scenario entries against the real compiled modules of dist (the leader worker topology electing and assigning across five fake agents, the planner executor separation, the critic review loop closing, the verifier confirming before completion, the message passing between every agent pair, the shared queue handing out every task exactly once, the work stealing under its lane lock protocol, the blackboard merging concurrent writes, the tab handoff keeping the session state on every browser kind, the shared resource lock blocking double writes, the conflict detection on simultaneous edits, the parallel result merge with provenance, the vote and consensus flow with its quorum, the kill switch stopping every agent at once, the pause of one agent leaving the others running, the sub agent depth limits, the escalation gate reaching the human, the review request flow, the interleaved run replay for audit, the comparison of competing outputs, the prioritized task lanes, the resource arbitration under contention, the worker scale by site load, the per agent budget limits stopping the right agent, the per agent scopes gating the right kinds, the aggregate report merging every contribution, the interleaved timeline ordering, the lessons deduplication and the pool coverage of 469 through 502 mapped onto real exported functions) with the fake clock mode keeping every run deterministic, the fake tab provider covering every browser kind, the deterministic artifact tests/artifacts/agentcert.json and the nonzero exit on any failed entry; the costcert gate of tests/costcert.mjs executing all nine accounting checks (the recorded run replay recomputing every cost line, the per agent token accounting matching the step logs, the shared accounting summing the agent totals, the budget alerts firing at their thresholds, the refunds on cancelled steps matching the provider rules that bill only the streamed chunks, the model routing costs landing on the right agent, the exported report reconciling to the total, no cost event missing from the audit trail and the reconciliation report with its exit answer) with the deterministic artifact tests/artifacts/costcert.json; and both gates wired into the package validate chain (validate:agents), the verify workflow steps and the workflowcheck permitted set.
- [x] 1.1.96: ship the dashdone completion of the multi agent dashboard — the dashdone family module (dashdone.ts as its own dist bundle and declaration entry outside the frozen index surface) shaping the multiagentview of every panel, the dashboardpage template of the one web design file gaining the multi agent overview with the topology status, the per agent status cards with live progress and controls, the shared queue view with its lane filter, the message flow view, the conflict and arbitration log, the cost per agent panel fed by the costcert totals, the escalation inbox with its human answer controls, the kill switch and pause controls, the timeline scrubber over the interleaved events, the aggregate report download, the read only rendering for observers without the run role and the empty states that guide the first multi agent run, the background shaping the view from the live swarm state through multiagentviewof inside the dashboard view response, the agent registry snapshot exporting the certified topology inventory of the six certified scenario families, the manifest test covering the dashboard surface permissions, the runtime policy accepting the certification paths and the packageextension gate verifying the dashboard bundle.
- [x] 1.1.96: document the certification — docs/agentcert.md with the automated checklist table, the fake clock and fake tab provider sections, the pool mapping of 469 through 502 and the rerun instructions, docs/agentscenarios.md with the six certified topologies (the leader worker scrape, the planner executor critic, the parallel form fill, the monitoring swarm, the competing extraction, the escalation and review) each with its topology diagram, its cost characteristics, its consent model and its fake tab setup, docs/dashdone.md with the dashboard panels, docs/costcert.md with the accounting verification method and the refund rule, the multi agent certification row in docs/releasegates.md with the required set sentence, the roadmap progress line update, the readme certified topologies note, the scenario links in docs/05.agentalgorithms.md, the coordination verification rows in docs/07.featureflowmatrix.md, the coordination suite runtime budget in docs/22.performancemodel.md, the fake clock mode in docs/configuration.md and the certification artifact inventory in docs/referencecatalog.md.

## Documentation completion of 1.1.97

The doccheck gate joined the validate chain with ten check families green: the kind coverage exact over the three hundred thirty five kind catalog, the kind fields and the schema valid examples, the doc links and the code fences, the reference tables mirrored against the cli, protocol, audit, tool, error and configuration sources, the twelve mermaid flow diagrams with module links, the changelog and the roadmap coverage and the readme surface claims. The kind, flow and reference documentation pages landed with their verified headers, the contribution guide carries the doccheck requirements, and the migration guide and the freeze document cross link the new pages.


## Repository consolidation of 1.1.98

The operator reorganization flattened the docs examples into the docs root, gathered the schema, cap, research and fixture data into tests/code and removed the caps, manifests, patches and .devthink directories; this release repaired every broken reference and finished the shape: the root carries only the typescript modules beside the standard metadata files, the companion.mjs script became the compiled companion.ts module with the host manifest template embedded and stamped at build, the container.mjs script became the heredoc the containerfile interns, the seven capability manifests became dist build artifacts derived from the frozen lists, the ten frozen schemas copy from tests/code into dist/schemas with the catalog labels following, the example set ships as the dist/fixtures artifacts the packages and the gates read, the umd example page stamps as dist/umd-example.html, the freeze artifact and the permission baseline moved to the tests root beside their gates, the npm files list shrank to dist, manifest.json, web, README.md and LICENSE, the orphaned planlint baselines, the wouter patch and the two dead doc generators left the tree, and the corrupted minified umd sourcemap line from the 1.1.93 family repaired itself into the minified body it always meant to record.

## Release candidate one of 1.1.99

The sweep gate joined the validate chain and recorded zero open blockers: the audit trails hold zero recorded failures since the 1.1.90 consolidation so the deduplication and the replay sets are empty, the source scans found no todo marker without an owner, no silent catch block, no unbounded loop without its termination in the body, no hardcoded origin outside the reviewed deep link catalog and the reserved example host families, no secret literal in the shipped sources or the compiled bundles and no deprecated platform api call, and the runtime checks answered the settings round trip, the cancel path rollback of the queued steps only, the kill switch escape hatch, the clean profile load contract through the built pages and the chromium smoke job, and the surface openings of every built page. The pool audit recorded the 626 item ledger: 623 implemented against the shipped symbol corpus, 3 planned (the screenshot diffing baseline, the inline confirmation chips family and the human confirmation for payments and destructive deletes family — the confirmation chips and the payment and delete confirmations already answer through the pagechip confirmation and the confirmpay and confirmdelete gates of the security family, the planned marker records the vocabulary distance), zero unknown, and the implemented floor of 590 holds. The verification matrix recorded 18 cells green with 100% coverage over the 335 kind catalog against the fake tabs of every browser kind, and the telemetry free verification recorded zero outbound attempts behind the block all proxy. No defect entry stays open from the sweep: the candidate gates stay green from this release on.

## The platform release of 2.0.0

The chain closed: every work item of this document stands done or carries its written supersession record. The 2.0.0 release folded the release candidate two scope into the platform release without a separate candidate — the readiness gate walks the twenty four release gates against the recorded evidence and answers the go decision in docs/readiness.md, the migration bridge ships the migrateplan command with the version one, automa, selenium, ui vision and tabular importers over the fixtures of dist/fixtures/importers, the example gallery ships the thirty six recipes with the runner green in the verify lane, the cli recipes command and the sidepanel one click import, and the closing stamps name the platform status in the readme, the changelog, the release notes and the roadmap progress line. The security review closed the three open code scanning alerts of the polynomial regular expression family at the source, the rubygems channel joined the pipeline family configuration beside npm, maven, nuget and the container, and the hard coded hashes left the sources for the generated build artifacts. No open work item remains from the 1.1.31 chain: the todo document closes with the chain complete.
